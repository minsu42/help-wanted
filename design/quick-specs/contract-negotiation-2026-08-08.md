# Quick Design Spec: 계약 협상 (흥정 시스템)

**Type**: New Small System
**System**: 계약 협상 (systems-index #9, Feature 레이어, Tier 0)
**GDD Reference**: 없음 — 이 프로젝트는 `/design-system` 정식 GDD를 스킵한다
(`design/gdd/game-concept.md` Next Steps 참조). 상위 컨셉은
`design/gdd/game-concept.md`의 Core Mechanics / MVP Definition 절.
**Date**: 2026-08-08
**Scope**: 의뢰인이 제시한 의뢰를 3개 축으로 흥정해 타결/결렬시킨다.
모험가 배정과 파견 판정은 포함하지 않는다.
**Estimated Implementation**: 4~5시간

---

> **2026-08-09 개정 — 이 문서 전체가 낡았다. 3축 → 2축, 잔금 미지급 폐지.**
>
> `design/gdd/game-concept.md`의 2026-08-09 전면 개정(Core Mechanics §4)이 이 스펙의
> 핵심 전제를 뒤집었다. **선불 축은 폐지되었고, 잔금 미지급(떼먹기) 판정도 함께
> 폐지되었다** — 선불 축의 페이오프가 그 판정이었으므로 둘은 같이 서고 같이 눕는다.
> 남는 축은 **보상 / 위험 고지** 둘뿐이다. `wealth`는 살아남는다 — 보상 축의 수용
> 범위를 여전히 정하기 때문이다.
>
> 그 대신 흥정이 **근거 기반**으로 확장된다: 청취·소문으로 알아낸 사실을 대사의
> 근거로 들면 요구 배율의 상한이 오른다. **이 확장의 구체 공식(근거 하나당 상한이
> 얼마나 오르는가, 근거 종류별 가중치)은 `game-concept.md`도 `production/roadmap.md`
> (P3)도 명시하지 않는다 — 미해결로 남긴다.**
>
> 아래 본문은 3축 시절 원문이며 무엇을 왜 결정했는지의 기록으로 남긴다.
> **살아남는 것**: 결정론 판정, 위험 고지 축의 개폐 조건과 `disclosureBonus`,
> 대사 선택 인터페이스(2026-08-09 인터페이스 개정), `durationDays`는 협상 대상이
> 아니라는 결정.
> **폐기되는 것**: 선불 축 전체, 잔금 미지급, "왜 세 축인가" 절의 결론(지금은
> 두 축).
>
> 근거: `design/gdd/game-concept.md` Core Mechanics §4 및 Risks and Open Questions
> §1, `production/roadmap.md` P0 / P3.

---

## Overview

의뢰인이 창구에 앉으면 플레이어는 **보상 / 선불 / 위험 고지** 세 축으로 계약 조건을
제안한다. 의뢰인은 자신의 숨은 상태(`wealth`, `urgency`, `hasAlternative`)에 따라
감내 가능한 총 부담량이 정해져 있고, 제안의 부담이 그 한도 안이면 수락한다.

판정은 **완전히 결정론적**이다. 난수가 개입하면 정보가 흥정력이 되지 못한다 —
아는 만큼 정확히 예측할 수 있어야 이 게임의 핵심 원리("정보 = 흥정력")가 성립한다.

> **인터페이스 개정 (2026-08-09): 슬라이더 → 대사 선택**
>
> 세 축·수용 공식·결정론은 **하나도 바뀌지 않았다.** 바뀐 것은 플레이어가 축을
> 움직이는 방법뿐이다. 슬라이더로 세 축을 맞춘 뒤 "제안한다"를 누르던 것을,
> **대사 하나를 고르면 그것이 곧 제안 한 번**이 되는 방식으로 바꿨다.
>
> 근거: 이 스펙 §4가 정의한 "반박 자체가 숨은 상태에 대한 정보다"라는 설계가
> 슬라이더에서는 카드 한켠의 문장으로 흘러가 버린다. 대화에서는 그 반박이
> **다음 수를 고르는 근거**로 되돌아온다 — 정보 채널이 인터페이스에서 실제로
> 성립하는 지점이다.
>
> 선택지 목록(도달 절대값)은 `balance.json`의 `negotiation.moves`, 문안은
> `text.json`의 `moves` 절에 같은 id로 있다. 한 수에 한 축만 움직이므로
> `maxOffers`를 2에서 3으로 올렸다 (아래 Tuning Knobs 참조).

### 왜 세 축인가 (기한 축을 뺀 근거)

> **2026-08-09 개정 — 세 축에서 두 축으로.** 아래 논증과 표는 **선불 축이 존재하던
> 시절의 기록**이다. 기한 축을 뺀 논증((b) 숨은 정보가 답을 바꿔야 축이 성립한다)은
> 그대로 유효하며, 선불이 빠진 것도 사실 같은 논증의 결과로 읽을 수 있다 — 선불의
> 정보값(`wealth`)은 보상 축에 흡수되었고, 선불의 페이오프였던 잔금 미지급 판정이
> 별도로 폐지되면서 축 자체가 남을 이유가 없어졌다.

축 하나가 값을 하려면 **(a) 다른 축과 구별되는 결정**, **(b) 숨은 정보가 답을 바꿈**,
**(c) 대가가 있음** 세 가지가 필요하다. (b)가 빠지면 그 축은 이 게임의 축이 아니라
그냥 슬라이더다.

| 축 | 어떤 숨은 정보가 답을 바꾸나 | 판정 |
|---|---|---|
| 보상 | `wealth`, `urgency`, `hasAlternative` 셋 다 | 기준축 |
| ~~선불~~ | ~~`wealth` — 떼일 것인가~~ | → **폐기 (2026-08-09).** 잔금 미지급 판정 폐지와 함께 제거 |
| 위험 고지 | 실제 위험도 — **정보가 없으면 축 자체가 안 열린다** | 정보 축 |
| ~~기한~~ | **없음** | 제거됨 |

기한 축에서는 *"아, 내가 알고 있었지"* 하는 순간이 발생하지 않는다. `urgency`가 얼마나
양보하는지를 정하긴 하지만 그것은 모든 축에 걸리는 공통 계수이지 기한만의 함정이
아니다. 게다가 기한을 협상 대상으로 두면 파견 판정(기한 페널티)과 모험가 명부(구속
일수) **두 시스템에 추가 결합**이 생긴다 — 정보 게임에 기여하지 않으면서 결합만
늘리는 축이다.

**기한을 없앤 것이 아니라 협상 대상에서 뺐다.** 소요 일수는 의뢰의 고정 속성
(`durationDays`)이고 모험가는 그만큼 묶인다. 일정 압박(경영 시뮬의 뼈대)은 그대로
남고, 슬라이더 하나와 결합 두 개가 사라진다.

남은 세 축은 **전부 정보 축**이다 — 축 구성 자체가 기둥 1("아는 자가 이긴다")을
말한다.

> **2026-08-09 추가 개정.** 아래 문단이 내린 결론(선불을 남기고 기한을 자른다)은
> 다시 뒤집혔다. 개정된 `game-concept.md`가 선불 축을 직접 폐지했고, Open Question 1은
> 이제 컨셉 문서 쪽에서 최종 종결됐다(`game-concept.md` Risks and Open Questions §1).
> 아래는 뒤집힌 결론의 기록이다.

> **컨셉 문서와의 차이**: `game-concept.md`의 Open Question 1은 "많으면 선불을 잘라
> 3개 축으로"라고 적었다. 이 스펙은 반대 결론을 내렸다 — 선불은 `wealth` 하나를 두
> 갈래 행동으로 가르는 정보 축이고, 잘라야 할 것은 비밀이 없는 기한 축이다.
> Open Question 1은 이로써 종결한다.

---

## Core Rules

### 1. 세 개의 축

> **2026-08-09 개정** — 선불 행 전체가 폐기됐다. 남는 것은 보상·위험 고지 두 축.

| 축 | 플레이어가 하는 일 | 의뢰인에게 주는 부담 | 플레이어가 치르는 대가 |
|---|---|---|---|
| **보상** | 기준 보상 대비 배율을 요구 | 요구 배율에 비례 | 없음 (순수 요구) |
| ~~**선불**~~ | ~~보상의 몇 %를 선지급받을지~~ | ~~선불 비율에 비례~~ | → **폐기 (2026-08-09)** |
| **위험 고지** | 실제 위험을 계약서에 명시 | 부담이 아님 — **허용치를 올린다** | `hasAlternative` 의뢰인은 떠날 수 있다 |

### 2. 위험 고지는 정보가 있어야만 열리는 축이다

플레이어가 해당 의뢰의 **실제 위험도를 소문으로 알아낸 상태**
(`PlayerKnowledge.revealedFacts`에 해당 사실 id가 있음)이고 **실제 위험도 > 공개
위험도**일 때만 이 축을 켤 수 있다. 모르면 UI에서 비활성이다.

이것이 "정보 = 흥정력"의 가장 직접적인 구현이다. 다른 두 축은 정보 없이도 조작할 수
있지만(불리할 뿐), 이 축은 **정보 없이는 존재하지 않는다.**

### 3. 수용 판정

> **2026-08-09 개정 — `w_advance` 항 제거, 근거 보너스 항은 미정.**
> 선불 축이 없으므로 `burden`에서 선불 항을 지운다. `tolerance`에는 근거 기반 흥정의
> 결과인 "근거 보너스" 항이 추가될 것으로 보이나, **근거 개수·종류가 상한을 얼마나
> 올리는지의 공식은 `game-concept.md`·`production/roadmap.md` 어디에도 없다.**
> 아래는 근거 항을 자리만 표시한 상태다 — 값을 만들지 않고 미해결로 둔다.

```
burden    = w_reward  × max(0, 보상배율 − 1)
          ~~+ w_advance × 선불비율~~ → 폐기 (2026-08-09)

tolerance = toleranceBase
          + wealthWeight  × wealth        // 부유하면 더 준다
          + urgencyWeight × urgency       // 급하면 더 준다
          − alternativePenalty            // hasAlternative일 때만
          + disclosureBonus               // 위험 고지를 켰을 때만
          + ???                           // 근거 보너스 — 공식 미정 (2026-08-09)

수락 = burden ≤ tolerance
```

`wealth`, `urgency`는 0~1 정규화 값이다 (`src/domain/types.ts`의 `Client`).
~~`선불비율`도 0~1이다.~~ `보상배율`은 1.0이 기준 보상.

### 4. 제안 횟수와 결렬

1. 플레이어가 대사를 하나 고른다 — 그것이 곧 제안 한 번이다. `burden ≤ tolerance`면
   **타결**.
2. 거부되면 의뢰인은 **기여도가 가장 큰 축 하나를 지목**해 반박한다
   (예: "그 값은 도저히 못 드리겠소"). 이 반박 자체가 숨은 상태에 대한 정보다.
   화면은 같은 축을 조건표에서도 강조해, 대사와 숫자가 같은 곳을 가리키게 한다.
   > **2026-08-09 개정**: 원문 예시("선불은 도저히 안 되겠소")는 폐기된 선불 축을
   > 가리킨다. 지목 가능한 축은 이제 **보상 / 위험 고지** 둘뿐이다.
3. 반박을 들은 뒤에는 **물러서는 수**가 열린다 (예: "그 조건은 없던 걸로 하죠").
   지목된 축을 중립으로 되돌리는 선택지이며, 이것이 없으면 대화가 결렬로만 끝난다.
4. `maxOffers`번째 제안이 거부되면 **결렬**. 그 의뢰는 사라지고, 숨겨져 있던 진실은
   영원히 밝혀지지 않는다.

`hasAlternative`이고 위험 고지를 켠 경우, 거부되는 즉시 반박 없이 **결렬**한다 —
다른 길드로 갔다. (부담이 허용치 안이면 고지는 오히려 타결을 돕는다. 고지 보너스가
허용치를 올려주고도 거부됐을 때만 이 규칙이 발동한다.)

### 5. 계약 이후 (다른 시스템으로의 출력)

- ~~**잔금 미지급**: 의뢰 완료 시 잔금 지급 여부는 `wealth`로 판정한다. `wealth`가
  낮으면 떼인다. **선불로 받은 몫은 무조건 확정이다** — 이것이 선불 축의 존재
  이유다.~~ → **폐기 (2026-08-09).** `economy.ts`의 `rng.chance(client.wealth)` 판정
  자체가 제거된다 (`production/roadmap.md` P0). 완수한 의뢰는 결정론적으로 전액
  지급된다.
- ~~**미지급의 학습 효과 보완**: 선불 축은 페이오프가 며칠 뒤에 오고 15일 회차에서
  떼이는 경험이 2~3번뿐이라 학습이 느리다. 완화책으로, 미지급이 발생하면 결과 대조
  화면에서 크게(붉은 봉랍색) 표시하고 **해당 의뢰인의 `wealth`를 영구 공개**한다.
  한 번 떼이면 그 사람에게는 두 번 다시 속지 않는다.~~ → **폐기 (2026-08-09).**
  전제(미지급 판정)가 사라졌으므로 이 보완책도 함께 사라진다. `WealthReveal`,
  `PlayerKnowledge.knownWealth`도 제거 대상이다.
- **침묵의 대가**: 실제 위험을 **알고도** 고지하지 않은 계약에서 사망이 발생하면,
  길드원들의 `trust`가 하락하고 `Memory.kind = 'wasDeceived'`가 기록된다.
  > **2026-08-09 재검토 필요.** 원문은 "신뢰가 낮으면 소문을 말해주지 않는다 —
  > 착취가 정보망을 스스로 조인다"고 이어졌으나, 소문의 출처가 길드원에서
  > 외부인·정보상으로 옮겨가면서(`design/quick-specs/rumor-network-2026-08-08.md`
  > 상단 개정 참조) 길드원의 `trust`가 더 이상 "사실을 말해주는가"를 게이트하지
  > 않는다. `trust` 자체는 살아남되(조언·자원·배정 수락에 쓰임 —
  > `production/roadmap.md` P4), **이 문장의 인과(신뢰 하락 → 정보망 축소)는
  > 다시 쓰여야 한다.** 대체 서술이 game-concept.md에도 명시되어 있지 않으므로
  > 미해결로 남긴다.
- **구속 일수**: 의뢰의 `durationDays` 동안 배정된 모험가는 `onMission` 상태가 된다.
  협상 대상이 아니라 의뢰의 고정 속성이다.

---

## Tuning Knobs

> **2026-08-09 개정** — `w_advance`는 폐기 대상이다. `maxOffers`는 "두 축(보상/
> 위험 고지)에서 반박·물러서기 왕복이 몇 번 필요한가"로 다시 검토해야 하나,
> 재산정값은 game-concept.md·roadmap.md에 없다 — 밸런싱 시점 판단으로 남긴다.

| Knob | Default | Range | Category | Rationale |
|---|---|---|---|---|
| `w_reward` | 1.0 | 0.5–2.0 | curve | 기준축. 나머지 가중치는 이것 대비로 읽는다 |
| ~~`w_advance`~~ | ~~0.8~~ | ~~0.3–1.5~~ | ~~curve~~ | → **폐기 (2026-08-09).** 선불 축 제거 |
| `toleranceBase` | 0.35 | 0.1–0.8 | gate | 정보 없이도 약간은 흥정되게 하는 하한 |
| `wealthWeight` | 0.5 | 0.2–1.0 | curve | 자금력이 보상 상한을 지배하지 않도록 억제 |
| `urgencyWeight` | 0.6 | 0.2–1.2 | curve | 급박함이 가장 큰 지렛대여야 흥정이 재밌다 |
| `alternativePenalty` | 0.25 | 0.1–0.6 | gate | 대안이 있으면 확실히 불리하되 협상 불가는 아니게 |
| `disclosureBonus` | 0.4 | 0.1–0.8 | gate | 정보 1개의 현금 가치. **밸런싱 1순위 노브** |
| `maxOffers` | 3 | 2–4 | feel | 대사 한 수에 한 축만 움직이므로 2로는 반박을 듣고 물러설 기회가 없다. 5 이상이면 시행착오로 정보 없이 풀린다. **2026-08-09: 축이 3→2개로 줄었으므로 이 값이 여전히 맞는지 재검토 필요 (미해결)** |
| `negotiation.moves` | 8개 | — | feel | 선택지 목록. 각 축의 도달 절대값이며 문안은 `text.json`의 `moves`에 같은 id로 있다. **2026-08-09: 선불 관련 이동값(`askAdvance`/`askAdvanceHalf`/`backDownAdvance`) 제거 대상 (`production/roadmap.md` P0)** |

전부 `src/data/balance.json`의 `negotiation` 섹션에 들어간다. 코드에 상수를 박지 않는다.

> **경로 주의**: 이 프로젝트는 템플릿 기본값인 `assets/data/`를 쓰지 않는다.
> `tsconfig.json`의 `include`가 `["src", "tests"]`이므로 `src/` 밖의 JSON은
> 타입체크 대상에서 빠진다.

---

## Affected Systems

| System | Impact | Action Required |
|---|---|---|
| 의뢰 생성 (#7) | 기준 보상, `durationDays`, 공개/실제 위험도를 공급 | `Contract` 타입에 해당 필드 정의 |
| 소문 네트워크 (#8) | `revealedFacts`가 위험 고지 축을 여닫는다 | 사실 id 규약 합의 필요 |
| 파견 판정 (#10) | **결합 없음** (기한 페널티를 제거했으므로) | 조치 없음 |
| 모험가 명부 (#5) | `durationDays` 동안 `onMission` | 상태 전이에 반영 |
| 명성·자금 경제 (#13) | ~~선불/잔금, 미지급 판정~~ → **폐기 (2026-08-09).** 완수 시 결정론적 전액 지급 | 자금 흐름에 반영 |
| 에이전트 시뮬레이션 (#3) | 침묵 후 사망 시 `trust` 하락 | `Memory.kind = 'wasDeceived'` 기록. **2026-08-09: 이 하락이 무엇을 잠그는지 재정의 필요 — §5 참조** |
| 창구 화면 (#14) | ~~3개 축 UI~~ → **2개 축 UI (2026-08-09)**, 위험 고지 비활성 상태 표현 | 비활성 **사유**를 보여줄 것 |
| 결과 대조 (#11) | ~~미지급 시 `wealth` 영구 공개~~ → **폐기 (2026-08-09)** | `PlayerKnowledge`에 반영 |

---

## Acceptance Criteria

- [ ] 같은 의뢰인 + 같은 제안이면 **항상 같은 결과**가 나온다 (난수 미개입)
- [ ] `urgency`가 높은 의뢰인은 낮은 의뢰인보다 더 큰 `burden`을 수락한다
- [ ] `hasAlternative` 의뢰인은 동일 조건에서 더 적은 `burden`만 수락한다
- [ ] 실제 위험도를 모르는 상태에서는 위험 고지 축이 **비활성**이다
- [ ] 실제 위험도 ≤ 공개 위험도이면 알고 있어도 위험 고지 축이 비활성이다
- [ ] 위험 고지를 켜면 동일 제안의 수용 범위가 `disclosureBonus`만큼 넓어진다
- [ ] 1회차 거부 시 **기여도 최대 축**이 지목된다 (다른 축이 지목되지 않는다)
- [ ] `hasAlternative` + 위험 고지 + 1회차 거부 → 반박 없이 즉시 결렬
- [ ] ~~미지급 발생 시 해당 의뢰인의 `wealth`가 `PlayerKnowledge`에 영구 기록된다~~
  → **폐기 (2026-08-09).** 미지급 판정 자체가 제거된다
- [ ] 모든 노브가 `balance.json`에서 읽힌다 — 코드에 리터럴 숫자 없음
- [ ] 회귀: 결렬된 의뢰의 실제 정보는 어떤 화면에도 노출되지 않는다

**필수 테스트**: `.claude/docs/technical-preferences.md`의 3대 필수 테스트 중 1번
("흥정 수용 판정 — 의뢰인의 숨은 상태별 조건 수용 범위")이 이 시스템이다.
위 항목 2·3·6을 단위 테스트로 고정한다.

---

## Tier 1 후보: 단골 계약

**기록해두는 이유**: 현재 설계에는 구조적 편향이 하나 있다 — **관대할 이유가 전혀
없다.** 세 축이 모두 더 뜯어내는 쪽을 가리키고, MDA의 Expression 축("정직한 중개인이
될지 착취자가 될지")을 지탱하는 것은 위험 고지 침묵으로 인한 `trust` 하락 하나뿐이다.

**아이디어**: 관대하게 타결한 의뢰인이 나중에 더 좋은 조건으로 돌아온다. 정직에
처음으로 기계적 보상이 붙는다.

**MVP에 넣지 않는 이유**: 15일 안에 재방문이 성립하려면 의뢰인 재등장 로직이 필요하고,
플레이어에게 "미래 가치 계산"이라는 새 인지 부하를 얹는다. 3일 안에 넣으면 반쯤
작동하다 말 확률이 높다.

---

## Systems Index

이미 등재되어 있다 — #9 계약 협상, Feature 레이어, Tier 0.
`design/gdd/systems-index.md`의 Design Doc 열을 이 스펙 경로로 갱신할 것.
