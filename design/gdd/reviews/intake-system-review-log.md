# Review Log — 청취 (Intake System)

대상 문서: `design/gdd/intake-system.md`

> ## ⚠ 2026-08-10 — **GDD 재작성 (v6).** 이 로그의 v1~v5 항목은 **다른 문서에 대한 것**이다
>
> 아래 리뷰·개정 기록은 전부 **v1~v5**를 대상으로 한다. 그 문서는
> `design/gdd/archive/intake-system-v1-v5-2026-08-10.md`로 보존됐고, 현재
> `intake-system.md`는 **컨셉을 다시 세운 재작성판**이다.
>
> **재작성 사유**: 다섯 판이 전부 *"대화를 하다 보면 의뢰서가 채워진다"* 였고,
> 입력 방식만 다섯 번 바뀌었다(자유 타이핑 → 카탈로그 → 그물+분류 → 앵커 →
> 흐름+생성 선택지). 플레이 판정이 매번 다음 판을 낳았다:
> *"Portopia 위험"* → *"정답 찾기"* → *"취조 같다"* → *"선택지 고르기 게임"*.
>
> **공통 원인은 의뢰서가 결과 표시판이었다는 것**이다. 재작성은 뒤집는다 —
> **의뢰서가 먼저이고 대화가 수단이다.**
>
> **살아남은 결정** (재리뷰 시 다시 검증할 필요 없음): 직업이 정하는
> `knows`/`tells` 두 축 · 무지/은폐 구별과 `rumorNothingToTell` 교훈 ·
> 하루 응대권 · 「창」 레지스터와 기색 · LLM은 생성만 · ADR-002 D9(휘발 상태) ·
> `topic` 필드 · 지갑 F4.
>
> **폐기된 결정**: 12노드 DAG · `gateCandidate` · 앵커 · 미끼 · 흐름 수명 ·
> 놓침 · 화법(공감/논리/압박) · 그 위에 세운 AC 다수.
> **Q8~Q13은 대상이 사라져 소멸**했고 새 Q1~Q6으로 대체됐다.

이 파일은 **개정 이력**이다. 재리뷰가 이전 판정과 미해결 항목을 추적할 수 있도록,
판정 당시의 지적을 **개정 후에도 지우지 않고 남긴다.**

---

## Review — 2026-08-09 — Verdict: NEEDS REVISION

Scope signal: **L**
Specialists: game-designer · systems-designer · ux-designer · ui-programmer ·
qa-lead · narrative-director · audio-director · **creative-director (종합)**
Blocking items: **8** | Recommended: **9** | Nice-to-have: 3
Prior verdict resolved: First review
완성도: 8/8 섹션 (표준 8개 + Visual/Audio · UI Requirements · Open Questions 추가)

### Summary

척추는 튼튼하다 — 소유권 경계가 깨끗하고, 결정론이 타입 수준에서 강제되며(`Rng` 미import),
`미상`/`막힘` 분리가 기둥 6에 정확히 물려 있다. 차단 항목은 전부 국소적이고 값싸며 구조
변경을 요구하지 않는다. 가장 중대한 발견은 **문안 질감의 스키마 부재**였다 — Player
Fantasy가 핵심이라 선언하고 이 저장소의 과거 사고(`rumorNothingToTell`)까지 인용한
`무지`/`은폐` 구별이, 정작 그것을 실어 나를 수단(#6)을 Dependencies에서 **Soft**로 표시하고
있었다. 판정은 돌지만 게임이 죽는 유형이다. 가장 날카로운 단일 발견은 **평평한 ±2%p
몬테카를로 허용 오차**로, 갱단의 4.7% 버킷에서 9.5σ에 해당해 망가진 구현도 통과시켰다.

### 구현 전 필수 8건 — 전부 이 세션에서 반영 완료 ✅

| # | 지적 | 출처 | 반영 |
| ---- | ---- | ---- | ---- |
| 1 | 응답 문안이 `(슬롯 × reach × limiter)` 5갈래를 요구하는데 `questions.json`은 질문당 `line` 하나. #6이 **Soft**로 표시됨 | narrative-director, systems-designer | ✅ **R10 신설**. 문안 소유를 질문에서 분리, #6 **Hard** 승격. AC-R10-01~03 |
| 2 | `종류` 클램프가 파생값 `reach`만 올려, 재질문 시 `limiter`가 클램프 이전 값으로 계산됨 | systems-designer | ✅ 상류 `knows`/`tells` 클램프로 명시. 상태 전이표에 무비용 행 추가. AC-F4-03 강화 |
| 3 | U7의 *".value 기록 지점은 둘"* 이 거짓 — 앵커 클릭이 세 번째이며 **조합 중에도 발생** | ui-programmer | ✅ 셋으로 정정 + `compositionend` 대기 규칙. AC-UI-05/06 신설 |
| 4 | (a) `막힘`인 선행 슬롯이 하위 질문을 영구 잠금 — 조용한 락아웃 재발 (b) 다부모 `requires` 의미론 미정의 | systems-designer | ✅ 충족 기준 `상태 ≠ 미상`으로 확장. **R8-2**(AND) 신설. AC-R8-02/03, AC-R8-2-01 |
| 5 | 평평한 ±2%p는 갱단 `확정=4.7%`에서 9.5σ. 가장 느슨한 분포(주민)만 검증 | qa-lead | ✅ 버킷별 `±3×SE`. **AC-F4-04**(갱단) 신설 |
| 6 | AC-CORE-04를 AC-DET-03과 같은 스텁으로 묶음 — 전자는 오늘 검증 가능 | qa-lead | ✅ 분할. AC-CORE-04 실테스트, AC-CORE-05만 스텁 |
| 7 | 한국어 부분 문자열 노이즈 케이스가 산문에만 있고 AC 0건 | qa-lead | ✅ **AC-EDGE-12** 신설 |
| 8 | 청취록 펼치기·접기에 키보드 경로 없음 — U6 자기 규칙 위반 | ux-designer | ✅ 접힌 가장자리를 `<button>`으로, `Tab`+`Enter`/`Space` |

### 같이 반영한 권고 4건 ✅

| 지적 | 출처 | 반영 |
| ---- | ---- | ---- |
| 가상 포커스 하이라이트 리셋 규칙 부재 → 후보가 줄면 **Enter가 엉뚱한 질문을 던진다** | ux-designer, ui-programmer (독립 동일 지적) | ✅ U6에 리셋 규칙 + AC-UI-07 |
| `모호`/`확정` 재질문에 소리·시각 신호 전무 (비용을 치른 행동인데) | audio-director | ✅ V5에 5번째 SFX 신설 |
| `paperwork-ux` §4.5의 반복 압축 약속과 재방문 규칙이 충돌 | game-designer | ✅ `paperwork-ux-2026-08-09.md` §4.5 정정 |
| 컨셉 문서의 세이브 스캐밍 방어(로그 재생)가 이 문서 결정으로 무효화 | 리드 리뷰어 | ✅ `game-concept.md` 지속성 절 정정 |

### 개정 중 추가로 발견해 고친 것 ✅

- **F3(인내심) 폐기가 덜 쓸렸다** — `patience`·`occupationBase`·`traitMultiplier`가
  Dependencies·상호작용 표 등 **6곳에 살아 있었고**, 이는 *"이 식별자들이 존재하지
  않아야 한다"* 는 **AC-F3-01과 정면 충돌**이었다. 전부 정리.
- AC-UI-01의 *"DOM에 실제로 반영된다"* → 구체 셀렉터·기대 텍스트로 재작성 (qa-lead 제안)
- AC-UI-04를 **「수동-BLOCKING」** 별도 차선으로 표기 + 증거 산출물·서명자 명시.
  happy-dom으로 검증 불가한 항목을 자동 BLOCKING과 같은 라벨로 두면 라벨이 거짓이 된다

### 미해결 — 다음 리뷰가 확인할 것 ⬜

| # | 지적 | 출처 | 왜 미뤘나 |
| ---- | ---- | ---- | ---- |
| R1 | **F5 최적 플레이가 생존을 악화시킬 수 있다.** 실제 위험이 저가중 슬롯(장소)에 몰리면 `completeness` 최대화가 위험을 놓친다 — 명시된 "안전" 3:1 비율 안에서도. 문서의 Q2가 다루지만 이 형태로는 안 적혀 있음 | systems-designer, creative-director | **P2 스펙의 구속 조건**으로 넘김. 기둥 6의 역전이므로 단순 공략화 문제보다 무겁다 |
| R2 | U7의 줄 수 추정 55~75줄이 낙관적 — 마운트 참조 캐싱, ARIA combobox 배선(이름만 불리고 미명세), V2 애니메이션 상태 추적 누락. 재추정 **120~180줄** | ui-programmer | 200줄 전환 트리거 아래이긴 하나 여유가 주장보다 적다. **구현 중 실측할 것** |
| R3 | 은폐형 P4 유보의 온램프가 문서에 안 적혀 있다 — 관리(`tells=none` 10%/슬롯)가 P1의 의도된 저강도 연습이라는 점 | game-designer ↔ creative-director (이견) | 문서에 한 줄 추가로 끝나지만 Q6과 함께 다루는 것이 맞다 |
| R4 | V6의 *"근거 1번은 반박 불가"* 가 같은 단락의 자기 우회로(비선율 앰비언스)와 모순. 예산 근거도 짧은 루프엔 미적용 | audio-director | P8 유보 유지. 다만 **기둥 2 때문에 이 화면이 사실상 게임 전체**이므로 P1 앰비언스 스코핑 권고가 살아 있다 |
| R5 | 재질문 시 완전히 동일한 문장이 영원히 반복되는 콘텐츠 피로가 계산되지 않음. 사유별 어휘 변형 2~3종이면 거의 공짜 | narrative-director | R10이 키 체계를 만들었으므로 **변형 추가가 이제 값싸다.** 문안 작성 시점에 결정 |
| R6 | 앵커 단어의 **시각 기표가 어디에도 명세되지 않음** — 무비용 클릭 경로 자체가 발견되지 않는다 | ux-designer | art-director 핸드오프. *"정적 산문과 구별된다"* 만 요구사항으로 두고 방법은 위임 |
| R7 | U4의 체크리스트 렌더링 금지가 AC에도 없고 「GATE로 넘긴다」 목록에도 없다 — 어느 게이트도 안 잡는다 | qa-lead | 다음 개정에서 둘 중 하나에 넣을 것 |
| R8 | V2의 200~300ms 필기 트랜지션은 새로 생성된 DOM 노드에서 CSS 전이가 안 걸린다 (2단계 클래스/rAF 필요) — 미명세 | ux-designer | 구현 세부. 조용히 *"애니메이션이 안 나온다"* 로 끝날 유형이라 코드 리뷰 체크 대상 |
| R9 | Q3(거짓 무지)이 R5의 *"limiter는 항상 진실"* 과 정면 충돌 — 채택 전에 해소해야 함 | narrative-director | GATE 이후 과제. **충돌을 지금 기록해 둔다** |

### 전문가 간 이견 (봉합하지 않고 남긴다)

- **클릭 앵커가 판타지를 훼손하는가.** game-designer는 앵커가 *어느 단어가 중요한지*
  미리 알려주는 소프트 드롭다운이라 봤고, creative-director는 앵커가 **질문을 고르는
  것이 아니라 받아 적는 것**이므로 과장이라 반박했다. → **타협 반영**: 시스템은 그대로
  두고, *"앵커 하나가 후보 하나와 1:1 대응되면 안 된다"* 를 **U2①의 데이터 작성 규약**으로
  넣었다. 1:1이 되는 순간 game-designer의 지적이 실제로 성립하기 때문이다.
- **은폐형 P4 유보가 절벽인가.** game-designer는 절벽, creative-director는 관리가 P1에
  남아 저강도 연습이 되므로 경사라고 봤다. → 미해결 R3.
- **청취록의 책상 자리 비용.** ux-designer가 스스로 축소 — 대사띠가 최신 답변을 공짜로
  보여주므로 마찰은 과거 답변 교차 참조 때만 발생.
- **R7의 키스트로크마다 필터링 성능.** ui-programmer가 스스로 비이슈로 결론.

---

## 2차 개정 — 2026-08-09 — 리뷰 후속 (판정 없음)

리뷰가 미해결로 남긴 **어휘 빈곤 문제**를 해결하며 규칙을 개정했다. 새 리뷰는 아니고
1차 리뷰의 후속 작업이다. 근거 문서:
`design/research/interrogation-games-market-2026-08-09.md` (시장조사).

### 무엇이 문제였나

리뷰의 game-designer가 *"카탈로그가 슬롯 단위 공용이라 몇 시간 안에 전부 학습된다"* 를
지적했고(1차 미해결 목록의 배경), 개정 중에 **더 강한 형태로 확인됐다**:

**R4(*"질문 하나가 상한까지 간다"*) + R10(문안 소유가 슬롯에 있다)를 같이 놓으면 같은
슬롯을 겨냥한 질문 둘은 완전히 같은 수가 된다** — 도달점도 같고 출력 문안도 같으므로
키워드 차이만 남는다. 즉 **슬롯당 질문은 사실상 1개**이고 카탈로그는 6개다.

그러면 Player Fantasy의 *"떠올리지 못한 여덟 개"* 와 *"이 시스템이 이 게임의 진짜 진행
시스템"* 이 8~15시간 캠페인에서 **거짓이 된다.**

### 해결 — 질문 깊이 등급 (선택지 (나))

| 변경 | 어디 |
| ---- | ---- |
| `reach = min(knows, tells, **depth**)` — 뭉툭(`vague`)/날카(`certain`) 2등급 | F1 · R4 |
| **뭉툭하게 열어야 날카롭게 파고들 수 있다** — 없으면 날카가 완전 지배 | **R4-2** 신설 |
| `limiter` 3값화 — **`질문`** 신설 (*"의뢰인이 아니라 내가 벽이었다"*) | F2 |
| "재질문"을 `depth ≤ 현재 상태`로 재정의 | R6 |
| 슬롯 어휘 **6개**, P1 의뢰 종류 **4종**, 카탈로그 **12개** | 「슬롯 어휘와 의뢰 종류」 신설 |
| 검산표 2체제 + **행동 칸 1개당 수익률** 표 | F4 |
| 문안 키 슬롯당 5종 → **6종**, 전체 36종 | R10 |
| **하루 행동 칸 = 의뢰인 × 슬롯 = 12** | Tuning Knobs |
| AC 13개 신설·개정 | AC 절 A·B |

### 시장조사가 결정을 뒤집은 지점 하나

검토 초기에 *"(나)가 안티 기둥 「정답이 있는 적발 퍼즐」에 (가)보다 가깝다"* 고 판단했다.
**L.A. Noire 사례가 그 반대를 보여줬다** — 정답 퍼즐이 되는 것은 선택지가 **하나의 옳은
값으로 붕괴할 때**이고, 비평가들의 처방이 *"틀린 선택에도 부분 정보가 나왔어야 한다"*
였다. 뭉툭한 질문이 `모호`를 주는 것이 그 처방이다. **(나)의 실제 리스크는 정답화가
아니라 "두 번 누르는 절차화"이며, 방어는 Q4 하나뿐이다.**

그리고 Ace Attorney의 `Press`(무비용 단일 등급 → *"전부 눌러라"* 가 공식 공략)가
**(가)의 미래를 그대로 보여준다.** 그 게임이 무너지지 않은 이유는 `Present`에 5회
페널티가 있어서다. **그래서 Q4는 (나)의 부속물이 아니라 전제 조건으로 격상됐다.**

### 우연히 맞아떨어진 것 — 기록해 둘 값어치가 있다

F4 수익률 표를 계산해 보니 **튜닝하지 않았는데** 기존 `w[slot]` 상한(3:1)과 깊이/넓이
비율(~3×)이 상쇄되어, **상인·관리는 깊이가 옳고, 귀족·갱단은 넓이가 옳고, 주민은 정확히
동점**이 된다. 직업을 읽고 전략을 바꾸는 것이 실제로 옳은 플레이가 된다(기둥 1).

⚠ **그러므로 `w[slot]` 3:1과 `depth` 2단계는 한 몸이다.** 어느 한쪽만 튜닝하면 균형이
깨진다 — Tuning Knobs 간섭 항목에 넣었다.

### 해소된 Open Question / 신설된 것

- **Q1 해소** — 카탈로그는 의뢰 종류가 아니라 슬롯 어휘에만 비례하고 어휘는 6개에서
  멈춘다. **Q1의 전제(선형 증가)가 틀렸다.**
- **Q4 잠정 확정** — 공식 `의뢰인 × 슬롯`. GATE 실측만 남았다
- **Q8 신설** — `limiter='질문'`이 억울함이 되지 않는가. **이번 개정의 가장 큰 수확이면서
  유일하게 검증되지 않은 감정**이고, 실패하면 기둥 6의 실패 모드에 걸린다.
  화면이 아무 표시도 하지 않기로 정했으므로(F2 완화안) **문안의 질감만으로** 전달돼야 한다

### 미해결로 남은 시장 리스크 — 설계 밖 ⬜

**배포 채널이 정해져 있지 않다.** 8~15시간 캠페인 + GitHub Pages 웹 배포는 itch에는
너무 길고, Steam에 올리려면 웹 전제(서버 없음 등)가 만든 기술 결정 상당수가 의미를 잃는다.
**로드맵에 이 결정이 없다.** (가)/(나)와 독립이지만 더 크다 — P8 이전에 답이 필요하다.

---

## Review — 2026-08-10 — Verdict: NEEDS REVISION → **개정 완료 (재리뷰 대기)**

Scope signal: **L**
Specialists: game-designer · systems-designer · ux-designer · ui-programmer ·
qa-lead · narrative-director · audio-director · **economy-designer** ·
**lead-programmer** · **creative-director (종합)** — 9종 + 종합
Blocking items: **6** | Recommended: **5** | Nice-to-have: 3
Prior verdict resolved: **Yes** — 1차(2026-08-09)의 필수 8건은 전부 반영된
상태였다. 이번 리뷰는 **3차 개정**(카탈로그 → 단서 그물 + LLM 분류) 대상이다.
완성도: 8/8 섹션

### Summary

**creative-director 판정: *"단서 그물 + LLM 분류라는 판돈은 옳다. 아래는 전부
이음매지 기초가 아니다."*** 필수 6건 중 어느 것도 그물 모델을 다시 열 것을
요구하지 않았다. 가장 값진 발견은 **여러 전문가가 독립적으로 같은 이음매를
짚은 것들**이었다 — 지연(4명), *"아무 일도 안 일어남"*의 구별 불가(3명).

### 구현 전 필수 6건 — 5건 반영, 1건 실측으로 기각

| # | 지적 | 출처 | 결과 |
| ---- | ---- | ---- | ---- |
| 1 | U9의 2~8초 LLM 왕복이 *"직접적이고 지연이 없어야 한다"*와 충돌 | game-designer · ux-designer · audio-director · ui-programmer **(4중 독립)** | ❌ **기각 — 실플레이 반증.** 사용자 판정: 지연이 체감되지 않았다. U9는 폴백 경로 사양으로 존치. 부수 지적(대기 중 입력 거동 미정의)만 반영 (U2) |
| 2 | *"아무 일도 안 일어남"*이 4종인데 소리로만 갈린다 — 소리 끄면 구별 불가 | game-designer · audio-director · ux-designer **(3중)** | ✅ **Edge Cases 「네 가지 무반응」 신설.** 티켓 뜯김·청취록 `✒`·로그 문구 3중 구별. AC-EDGE-06 |
| 3 | F1이 적힌 대로 구현 불가 — 동시 충돌 무순서·`게이트 후보` 미정의·축퇴 그물 미검증·슬롯 멤버십 미검사 | systems-designer | ✅ **①~④ 우선순위 규칙(무지 우선)**, `gateCandidate` 스키마 정식화, R3 검증 4항목. AC-F1-04/05, AC-R3-01 |
| 4 | 지속성이 "완료"로 표기됐으나 캐시 수명·NFC 정규화 미정 | lead-programmer · qa-lead | ✅ **R10 재작성** (캐시 = 세이브 포함, 키 = NFC). ADR-002 개정 범위 확장, ADR-003 행 강등. AC-R10-03/04 |
| 5 | F6 계층 역전(`counter < easy`) + 역제시 무비용 지배 전략 + `cap` 분포표 부재 | economy-designer | ✅ **불변식 `0 < easy < counter ≤ cap`**(빌드 게이트), 역제시 2회차부터 −1, **`walletMult` 직업별 표**. AC-F6-02/03, AC-R12-02 |
| 6 | AC 결함 4건 + 누락 2건 | qa-lead | ✅ AC-R8-03(5종→3종)·UI-04(DOM 훅)·DET-02(데이터 정합)·R12-01(범위) 수정, **AC-R9-01·AC-UI-08 신설** |

### 권고 5건 — 전부 반영 ✅

| 지적 | 출처 | 반영 |
| ---- | ---- | ---- |
| Q3(거짓 무지)이 AC-CORE-04의 비대칭과 충돌 — GATE 이후는 너무 늦다 | game-designer | Q3을 **GATE 이전으로 상향** |
| R11이 "매번 다르게"와 "질감은 항상 같게"를 둘 다 주장만 함. 발명 누출 하네스 없음 | narrative-director | **가변축/불변축 표** 신설 + **누출률 하네스** 신설 (기준선은 Q10) |
| U7의 *"경계 유지가 쉬워졌다"*가 자기 델타로 지지되지 않음 | ui-programmer | 문장 **삭제**, 1차 미해결 R2가 열려 있음을 명시 |
| IME 조합 × 앵커 클릭 × 재렌더 **3중** 케이스 미정의 (쌍만 있음) | ui-programmer | **U7-2 신설** — 큐 수명을 입력 상자에 묶어 판정 지점을 하나로 |
| R12의 "시세의 네 배 = 경보"가 시세 학습 동선 없이는 무의미 | narrative-director | Dependencies에 **순서 의존** 명시 (첫 계약 전 시세표 1회 노출 요구) |

### 전문가 간 이견 — creative-director 판정

- **무지/은폐에 다른 SFX를 줄 것인가.** audio-director는 판별 훈련을 약화한다고
  봤고, creative-director는 **기각** — 문서가 이미 그 반론을 예상하고 근거(소리가
  문장보다 먼저 도착해 텔레그래핑한다)를 남겨 뒀다. **단 audio-director의 별개
  발견(R5/R6 비대칭)은 채택**되어 필수 2번이 됐다.
- **R12를 잘라낼 것인가.** game-designer는 스코프 크립으로 봤고, creative-director는
  **유지** — 이번 개정 최고의 장면이라는 판단. 대신 economy-designer의 F6 수정을
  조건으로 달았고, 그대로 반영됐다.

### 같은 세션에서 함께 일어난 것 — 아트 방향 전환 🎨

리뷰와 별개로 **창구가 「창」 레지스터를 갖게 됐다.** 실플레이에서 *"너무 밋밋하게
텍스트처럼 진행된다"*는 판정이 나왔고, 그것이 **리뷰의 반복 지적(기색을 놓치면
문이 있는 줄도 모른다)과 같은 곳을 가리켰다** — 기색을 실어 나를 채널이 대사 문안
하나뿐이었다는 것.

- `design/art/art-bible.md` — **§1이 2분법 → 3분법**(종이·**창**·방), §5 제목 변경,
  **§5.9 신설**, §10-1·§11 갱신
- 이 문서 — **V0 신설**, V3에 표정 순서 규칙, V7 3분법 갱신, U1 레이아웃,
  AC-UI-09 신설, **Q11 신설**(창이 다른 화면으로 번지는가)
- `docs/asset-credits.md` — 「창구 인물 검토」 절 신설. **기존 Ninja Adventure 팩
  (CC0, faceset 50명+) 채택**, 별도 초상 팩 5종 기각 기록

> **기록해 둘 실수 하나**: 처음에 별도 초상 팩(DezrasDragons)을 **실물을 안 보고
> 설명만 읽고 추천**했다가, 시트를 열어 보고 화풍 불일치·캐스트 부적합으로
> 철회했다. `asset-credits.md`에 *"설명만 보고 채택하지 말 것"*으로 남겼다.

### 미해결 — 다음 리뷰가 확인할 것 ⬜

| # | 항목 | 왜 미뤘나 |
| ---- | ---- | ---- |
| N1 | **Q9 — 기둥 1의 수치 재성립.** 그물 모델에서 "직업을 읽는 것이 옳은 플레이"가 숫자로 서는지 | 그물 템플릿 실물이 있어야 계산이 선다. **재검산 없이 P1 데이터를 확정하면 안 된다** |
| ~~N2~~ | ~~**ADR-002 개정** — 그물 지속 상태 + 분류 캐시~~ → ✅ **완료 (같은 날).** 아래 참조 | — |
| N3 | **1차 리뷰 R2 (U7 줄 수 120~180)** — 여전히 열려 있음. 3차 개정이 분기를 늘려 순증감 불명 | 구현 중 실측만이 답이다. 200줄 = Preact 트리거 |
| N4 | **1차 리뷰 R4 (V6의 근거 1번 자기모순)** — 3차에서도 손대지 않음 | P8 유보 유지 (audio-director 재확인) |
| N5 | `systems-index.md` 미반영 — #24 재정의(질문 카탈로그 → 단서 그물 템플릿), #26 4권 구성 | 후속 작업 |
| N6 | **표정 4종·어깨 합성 파이프라인** 미구현. faceset 추출을 24 → 50명+로 확장 필요 | `tools/asset-pipeline/` 별도 스토리 |

### 후속 — ADR-002 개정 (2026-08-10, 같은 날) 📐

필수 4번의 후속으로 ADR-002를 개정했고, **결론이 리뷰의 예상과 반대로 나왔다.**

lead-programmer는 *"분류 캐시 수명이 미정의다 — 메모리 전용이면 복원 후 재호출이
나가 결정론이 조용히 깨진다"* 고 지적했고, 나는 그 지적을 받아 **"캐시를 세이브에
포함한다"** 로 GDD를 고쳤다. **그것이 틀렸다.**

ADR-002를 열어 D5(저장 시점은 하루 마감뿐)와 대조하니:

```
저장은 하루 마감에만 일어난다 (ADR-002 D5)
칸이 떨어지면 대기 중이던 의뢰인은 그냥 사라진다 (game-concept.md:286)
같은 의뢰인이 다시 와도 그물은 새로 구워진다 (intake GDD Edge Cases)
──────────────────────────────────────────────────
∴ 저장 순간에는 창구에 사람이 없다 → 건널 경계가 자체가 없다
```

**지적은 옳았다(수명이 미정의였다). 답이 반대였을 뿐이다** — 넣는 것이 아니라
**"안 넣는다"를 명시적 결정으로 적는 것**이 답이었다.

| 무엇 | 결과 |
| ---- | ---- |
| **ADR-002 D9 신설** — 「휘발 상태를 명시한다」 | 그물·청취록·분류 캐시는 저장하지 않는다. 대가(하루 중간 이탈 시 그날 손실)와 깨지는 조건(중간 저장 도입 시 D5·D6까지 함께 열림)을 명시 |
| **뜻밖의 수확** | **세이브에 LLM이 만든 값이 하나도 없다**는 성질이 공짜로 나왔다. ADR-003 채택 시 열려 있던 *"세이브가 비결정성에 오염되는가"* 가 구조적으로 닫혔다 |
| `schemaVersion` | **1 유지** — `save.ts`·`Rng.snapshot()`이 아직 미구현이라 세이브 파일이 세상에 0개다. ADR이 스스로 부른 *"마이그레이션 비용 0인 시점"* 이 아직 유효 |
| GDD 되돌림 | R10 캐시 문단 교체, AC-R10-04 반전(+AC-R10-05 신설), 「ADR 대상」 절 축소. **ADR-003 행이 ⚠에서 ✅로 복귀** (강등 사유가 D9로 소멸) |
| ADR-002 낡은 참조 정리 | 재현 키(질문 id → 분류 결과), `questions.json`→`quest-templates.json`, `limiter`→사유, `AC-R9-01` 오참조, 「다음 ADR 후보」의 ADR-003 번호 충돌 |

> **다음 사람에게**: 리뷰 지적을 반영할 때 **지적이 가리킨 문서만 보고 고치지 말 것.**
> 이 건은 GDD만 보면 "캐시를 저장한다"가 자연스러운 답이었고, **ADR을 열어 봐야
> 반대라는 것이 보였다.** 필수 4번이 애초에 *"ADR-003을 완료로 표기한 것이
> 성급하다"* 는 형태였던 것이 힌트였다 — **경계 문제는 항상 두 문서 사이에 있다.**

---

### 다음 리뷰를 위한 메모

이 문서는 **정식 8섹션 GDD로 승격된 첫 사례**다(다른 시스템은 `design/quick-specs/`
경량 스펙). 재리뷰 시 `systems-index.md`의 #21 행과 `production/roadmap.md`
「문서 작업 목록」이 그 승격을 반영하고 있는지 먼저 확인할 것 — 이 리뷰 세션에서
인덱스는 정정했다.
