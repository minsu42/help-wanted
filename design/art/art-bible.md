# Art Bible — Help Wanted

> **상태**: 적용 중 (길드 홀 완료, 창구/의뢰서 등 나머지 화면 대기 — 2026-08-09
> 개정으로 대기 화면의 범위가 늘어남, 2026-08-10 개정으로 창구의 시각 방향이 바뀜)
> **최종 수정**: 2026-08-10
> **상위 문서**: `design/gdd/game-concept.md`의 Visual Identity Anchor
> **에셋 출처**: `docs/asset-credits.md`

---

> **2026-08-09 개정 — 창구의 새 요소(청취·의뢰서·단서 토큰·길드마스터북·행동
> 횟수·세력 평판) 반영.**
>
> `game-concept.md`가 전면 개정되며 창구가 3막(청취 → 의뢰서 작성 → 협상)이
> 됐고, **의뢰서가 화면의 중심**이 됐다. 이 문서는 §1~§4(레지스터 구분,
> 팔레트, 붉은색 규율)를 그대로 유지한 채 **새 §5("종이의 규칙")를 추가**해
> 새 화면 요소의 시각 규칙을 정의한다. 이하 섹션 번호는 새 §5 삽입으로
> 하나씩 밀렸다 — 옛 §5(방의 규칙)는 이제 §6이다.
>
> 두 곳에서 기존 3원칙과 정면으로 부딪혔다 — **자동완성 드롭다운**(종이 위의
> 사물이 아닌 현대 UI 관례)과 **단서 토큰의 타입 구분**(색을 하나 더 쓰자는
> 요구가 "강조색은 하나뿐이다"와 충돌). 두 충돌과 해소안은 §5.4, §5.5에
> 각각 적어둔다.

---

> ## ⚠ 2026-08-10 개정 — 세 번째 레지스터 「창」 신설. §1의 2분법이 3분법이 된다.
>
> **무엇이 바뀌었나.** 창구 화면에 **창 너머로 보이는 픽셀 인물**이 들어온다.
> 의뢰인이 글자로만 존재하던 것을 그만두고, Papers, Please의 부스 창처럼 **전용
> 뷰 프레임**을 만들어 그 안에 상반신을 앉힌다.
>
> **왜 §1을 건드려야 했나 — 정직하게 남긴다.** 원래 §1은 창구를 **종이 레지스터에
> 통째로** 넣고 *"창구에 픽셀 타일을 깔지 마라"*를 핵심 규율로 못박고 있었다.
> §11이 창구의 현재 픽셀 실루엣·창틀을 *"섞여 있다 — 정리가 필요하다"*고 적은 것도
> **종이 쪽으로 정리하라**는 뜻이었다. 이번 결정은 그 방향을 뒤집는다.
>
> **뒤집은 근거.** 실플레이에서 창구가 *"너무 밋밋하게 텍스트처럼"* 읽힌다는 판정이
> 나왔다. 그리고 청취 GDD 3차 개정이 **기색**(말끝 흐림·시선 회피 — 은폐 게이트의
> 유일한 신호, R8)을 핵심 기제로 올렸는데, `/design-review` full 리뷰에서
> **"기색을 놓치면 문이 있는 줄도 모른다"가 반복 지적**됐다. 기색을 텍스트로만
> 전달하는 것은 설계상 불리하며, **얼굴이 있으면 이 문제가 시각으로 풀린다.**
> 즉 이 개정은 미감이 아니라 **게임 기제가 요구한 것**이다.
>
> **지킨 것 — 규율이 완화된 것이 아니다.** 창구가 "방"이 되는 것이 **아니다.**
> 카운터 상판을 경계로 위는 창(사람), 아래는 종이(서류)이며, §5.1의
> **"의뢰서가 화면의 중심"은 불변**이다. 길드 홀의 탑다운 픽셀을 창구로 옮겨오는
> 것도 아니다 — 창은 **홀도 종이도 아닌 제3의 요소**이고, 그래서 새 레지스터로
> 선언한다. 「나갔다가 책상으로 돌아온다」는 리듬은 **창구 안에서 위/아래로
> 재현**된다.
>
> | 절 | 효력 |
> | ---- | ---- |
> | **§1** | 레지스터 표에 **창** 한 줄 추가. 2분법 → 3분법 |
> | **§5 제목** | 「종이의 규칙」 → **「창구의 규칙 (종이와 창)」** — 절 번호는 바꾸지 않는다(교차 참조 보호) |
> | **§5.9** | **신설** — 창 너머의 사람. 프레임·크기·기색 표정·에셋 규약 |
> | **§5.1** | **불변** — 의뢰서 중심성은 이 개정으로 흔들리지 않는다 |
> | **§10-1** | 설계 테스트 1번을 3분법으로 갱신 |
> | **§11** | 창구 항목 재작성 (정리 방향이 뒤집혔다) |
>
> 결정 근거 기록: `design/gdd/intake-system.md` Q11,
> `design/gdd/reviews/intake-system-review-log.md` (2026-08-10 리뷰).

---

## 1. 한 줄 규칙

**종이 위에서 셈하고, 창 너머로 사람을 보고, 방 안에서 만난다.**

이 게임에는 성격이 다른 세 종류의 화면 요소가 있고, 셋은 **의도적으로 다르게
생겼다.**

| 레지스터 | 어디 | 무엇처럼 보이는가 | 플레이어가 하는 일 |
|---|---|---|---|
| **종이** | 창구 하단, 결과 대조, 결산 | 책상 위의 양피지 문서 | 읽고, 따지고, 서명한다 |
| **창** *(2026-08-10 신설)* | 창구 상단 | 창틀 너머의 픽셀 상반신 | **본다.** 얼굴을 읽는다 |
| **방** | 길드 홀 | 위에서 내려다본 픽셀 공간 | 가서, 말을 건다 |

세 레지스터를 섞지 않는 것이 이 문서의 핵심 규율이다. 길드 홀을 양피지 카드
목록으로 만들거나, **창구 하단(의뢰서 쪽)에 픽셀 타일을 깔거나**, 창 안에 서류를
띄우면 **"정보를 캐러 나갔다가 책상으로 돌아온다"는 회차의 리듬이 시각적으로
사라진다.**

**창과 방은 둘 다 픽셀이지만 같은 것이 아니다.**

| | **창** (창구) | **방** (길드 홀) |
|---|---|---|
| 시점 | **정면.** 눈높이에서 마주 본다 | **위에서 내려다본다** |
| 인물 | 상반신 1명, 고정 | 전신 여러 명, 배치됨 |
| 플레이어의 위치 | **안쪽.** 창은 나와 상대를 가르는 경계다 | 바깥. 내가 가서 말을 건다 |
| 조작 | **없다 — 보기만 한다.** 클릭 대상이 아니다 | 사람을 눌러 말을 건다 |
| 하는 일 | **얼굴을 읽는다** (기색) | **누구에게 갈지 고른다** |

시점이 다른 것이 이 구분의 핵심이다. 홀에서 플레이어는 **돌아다니는 사람**이고,
창구에서는 **앉아서 맞는 사람**이다. 같은 픽셀이어도 카메라가 다르므로 두 화면은
섞이지 않는다.

> **2026-08-09 확인**: 이 규율은 이번 개정으로 오히려 더 중요해졌다. 청취·
> 의뢰서·단서 토큰·길드마스터북·행동 횟수·세력 평판이 전부 **종이** 레지스터에
> 속한다 (아래 §5). 새 요소가 늘어난 쪽은 방이 아니라 종이이므로, 종이 쪽의
> 규칙을 §5로 별도 절을 만들어 다룬다.
>
> **2026-08-10 정정**: 위 문장의 *"창구가 통째로 종이"*라는 전제가 깨졌다.
> 창구는 이제 **위아래로 두 레지스터를 갖는 유일한 화면**이다 — 카운터 상판이
> 그 경계선이다. 다만 §5.1(의뢰서 중심성)은 불변이며, 창은 화면 **상단의 띠**로
> 제한된다. 자세한 규칙은 §5.9.

---

## 2. 원래 방향에서 무엇이 바뀌었나

`game-concept.md`의 「양피지와 봉랍」 3원칙 중 **하나를 명시적으로 뒤집었다.**

### 뒤집은 것 — "얼굴을 그리지 않는다"

> 원문: *"캐릭터 일러스트 없음. 인물은 이름, 필체, 그가 남긴 기록으로만 존재한다."*

**지금은 얼굴을 그린다.** 방 안의 인물 스프라이트와 대화창의 초상이 있다.

**왜 뒤집었나.** 원칙의 근거는 "3일 안에 30명을 작화할 수 없다"는 **제작 비용**이었지
미학적 신념이 아니었다. CC0 팩에서 24명분의 얼굴을 19KB에 확보하면서 그 전제가
사라졌다. 그리고 얼굴이 생기자 얻은 것이 분명하다 —

- **길드 홀이 목록에서 장소가 됐다.** 세로 목록은 "명부를 읽는" 화면이지만, 방은
  "가서 말을 거는" 곳이다. 정보를 캐는 행위가 목록 조작이 아니라 **누구에게 갈
  것인가의 선택**으로 보인다.
- **성격 필터의 학습이 얼굴에 걸린다.** *"저 친구 말은 늘 과장이더라"* 가 성립하려면
  그 친구가 기억에 남아야 한다. 이름 여섯 글자보다 얼굴이 훨씬 잘 남는다.

### 지킨 것

1. **모든 정보는 종이 위에 있다** — 창구·결과·결산은 그대로 양피지다.
2. **강조색은 하나뿐이다** — 붉은 봉랍색(`--seal`)은 위험·사망·결렬에만.
   이 규율은 오히려 더 엄해졌다 (§4 참조). **2026-08-09 확인**: 단서 토큰
   타입을 색으로 나누자는 요구가 있었으나 이 원칙을 지키기 위해 기각했다
   (§5.5).

### 2026-08-09 확인 — 세 번째 원칙이 하나 더 생겼다

**의뢰서가 화면의 중심이 된다.** 이번 개정으로 창구의 주인공이 대화창이 아니라
**작성 중인 서류**가 되었다. 아트 방향은 이것을 반영해야 한다 — 서류가 가장 크고,
가장 촉각적이고, 도장을 찍는 순간이 가장 확정적이어야 한다. 상세 규칙은 §5.1.

---

## 3. 팔레트

### 기본 (양피지와 봉랍)

`src/presentation/styles/base.css`가 유일한 출처다. 여기 없는 색을 화면 CSS에서
새로 만들지 않는다. **2026-08-09 확인**: 새 UI 요소(청취 입력창, 자동완성,
단서 토큰, 길드마스터북, 행동 횟수 표시기, 세력 평판 화면)도 예외 없이 이
표의 색만 쓴다 — 새 색을 추가하지 않는다 (§5.5, §5.8 참조).

| 변수 | 값 | 쓰임 |
|---|---|---|
| `--paper` | `#e8dcc0` | 양피지. 문서의 바탕 |
| `--paper-dim` | `#d6c8a6` | 바랜 종이, 부차 정보. **2026-08-09**: 청취 입력창·자동완성 목록의 바탕으로도 쓴다 (§5.3, §5.4) |
| `--desk` | `#3a2c1e` | 책상. 화면 바탕 |
| `--desk-dark` | `#2a1f14` | 책상 그늘, 테두리 |
| `--ink` | `#2b2118` | 본문 잉크. **2026-08-09**: 의뢰서 판단 칸, 단서 토큰 각인색으로도 쓴다 |
| `--ink-faint` | `#6b5a45` | 흐린 잉크. 라벨, 보조. **2026-08-09**: 의뢰서 받아쓰기 칸, 추천값 미리보기 |
| `--seal` | `#8b2320` | **붉은 봉랍. 위험 전용** |

### 방 (길드 홀)

방은 에셋 팔레트를 따르되 **화덕 불빛 색 하나만** 추가로 쓴다.

| 색 | 값 | 쓰임 |
|---|---|---|
| 화덕 불빛 | `#c9924a` / `rgb(255 186 94 / 30%)` | 선택 표시, 대사 인용 띠, 방 조명 |
| 외부인 이름 | `#f0c079` | 불빛 계열. 소속의 **두 번째** 신호 |
| 방 어둠 | `rgb(10 7 4 / 62%)` | 가장자리 비네트 |

화덕색은 봉랍색이 아니다. **따뜻하고 안전한 쪽**이며, 위험을 뜻하지 않는다.

---

## 4. 붉은색 규율

> **붉은 봉랍색이 나타나는 순간 플레이어의 심장이 뛰어야 한다.**

허용:

- 사망, 사망자 명단
- 협상 결렬
- ~~잔금 미지급~~ → **폐기 (2026-08-09)**. `game-concept.md` 개정으로 잔금
  미지급 자체가 제거됐다 (선불 축과 함께 폐지). 이 항목이 쓰던 붉은 표시는
  이제 등장하지 않는다.
- 실제 위험을 알고도 숨긴 것 (은폐 표식)
- 위험 고지 선택지 — 되돌릴 수 없고 대안 있는 의뢰인에게는 결렬로 이어지는 유일한 수

금지:

- **비활성 사유에 쓰지 않는다.** 자금 부족·정원 초과는 위험이 아니라 "아직 안 된다"다.
  **2026-08-09 확인**: 하루 행동 횟수가 바닥나는 것도 이 규칙에 든다 — 압박이지
  위험이 아니다 (§5.7).
- **거부·반박에 쓰지 않는다.** 의뢰인이 축을 걸고넘어지는 것은 **정보**이지 위험이
  아니다. 굵은 먹선(`--desk`)으로 강조한다.
- **길드 홀 전체.** 이 화면에는 사망도 결렬도 없으므로 `--seal`이 한 번도 등장하지
  않는다. 여기서 쓰면 다른 화면에서 그 색이 갖는 무게가 깎인다.
- **단서 토큰의 타입 구분에 쓰지 않는다.** 토큰 타입 중 하나가 "위험" 계열이라도,
  그것은 실제 위험 확정이 아니라 **단서 하나**일 뿐이다. 색이 아니라 도장
  모양으로 구분한다 (§5.5).

*설계 테스트*: 이 요소가 붉어야 할 만큼 치명적인가? 아니면 세피아다.

---

## 5. 창구의 규칙 (종이와 창) *(2026-08-09 신설 · 2026-08-10 확장)*

> `game-concept.md` 개정으로 창구의 주인공이 대화창에서 **작성 중인
> 의뢰서**로 바뀌었다. §1의 "종이" 레지스터에 속하는 화면 중 화면 점유율이
> 가장 큰 요소가 되었으므로, §6(길드 홀 방 규칙)과 대칭으로 전용 절을 둔다.
> 아래 규칙은 창구 화면(청취·의뢰서·흥정), 하루 마감 화면, 세력 평판 화면에
> 적용된다.
>
> **2026-08-10 — 절 제목이 「종이의 규칙」에서 바뀌었다.** 창구가 종이와 창
> 두 레지스터를 갖게 되었기 때문이다(§1). **절 번호는 일부러 바꾸지 않았다** —
> `intake-system.md`·`commission-form.md`·구현 커밋이 §5.1·§5.2·§5.5를 이름으로
> 참조하고 있어, 번호를 밀면 그 참조가 전부 조용히 어긋난다. 2026-08-09 개정이
> 옛 §5를 §6으로 밀면서 이미 한 번 치른 비용이다. **§5.1~§5.8은 여전히 종이의
> 규칙이고, 창의 규칙은 §5.9 하나에 모여 있다.**

### 5.1 의뢰서가 화면의 중심이다

의뢰서는 화면에서 **가장 크고, 가장 촉각적이고, 도장을 찍는 순간이 가장
확정적**이어야 한다. 창구 화면의 레이아웃 우선순위:

1. 의뢰서 — 최대 면적, 항상 보인다
2. 의뢰인의 말 — 의뢰서 위 또는 옆의 대사띠. 새 창을 열지 않는다
3. 행동 버튼(질문한다 / 제안한다 / 도장을 찍는다) — 의뢰서 가장자리에 붙는다

*설계 테스트*: 새 창구 UI 요소가 의뢰서보다 시각적으로 무거운가? 그렇다면
의뢰서를 가리고 있다는 뜻이다 — 다시 배치한다.

### 5.2 받아쓰기 칸과 판단 칸은 다른 잉크로 쓴다

`design/research/paperwork-ux-2026-08-09.md` §4.1의 구조를 그대로 시각화한다.
둘을 **같은 잉크색으로 그리면 안 된다** — 플레이어가 "내가 쓴 것"과 "자동으로
채워진 것"을 한눈에 구별해야 기둥 6(*"내가 쓴 것이 사람을 죽인다"*)이
시각적으로도 성립한다.

| 칸 | 잉크 | 근거 |
|---|---|---|
| 받아쓰기 칸 | `--ink-faint` (흐린 잉크), 서기체 느낌 | 자동 기입 — 사무적으로 보여야 한다 |
| 판단 칸 | `--ink` (본문 잉크) | 결정 — 손으로 눌러 쓴 것처럼 보여야 한다 |

빈 판단 칸(아직 등급을 안 고름)은 **점선 칸**으로 비워 둔다. 추천값이 있으면
`--ink-faint`로 미리 채워 두되(paperwork-ux §4.5), 플레이어가 직접 고르는
순간 `--ink`로 굳는다 — **잉크가 짙어지는 것 자체가 "이제 이건 내가 쓴
것이다"라는 피드백**이다.

### 5.3 청취 입력창 — 여백의 쪽지, 대화창이 아니다

입력창을 말풍선이나 채팅창처럼 그리지 않는다. **책상 위의 메모지 한 장**처럼
그린다 — 의뢰서 옆에 붙은 작은 양피지 조각.

- 배경은 `--paper-dim`. 창구 화면 전체가 이미 `--paper`이므로, 입력창이
  살짝 더 어두워야 "이건 초안이다"라는 위계가 생긴다.
- 테두리는 얇은 잉크 선 하나로만 표시한다. 상자 그림자를 넣지 않는다.
- 자유 타이핑 중에는 **아무 후보도 보이지 않는다** — paperwork-ux §4.3②의
  핵심 설계를 시각적으로도 지킨다. 입력창에 검색 아이콘·물음표 아이콘 등
  "찾아보라"는 초대장을 넣지 않는다. 이 게임은 **떠올리지 못한 것에 대한
  후회**를 원한다.

*설계 테스트*: "종이에 쓸 수 있는 것인가?" → **통과.** 메모지는 이미 책상
위에 있는 사물이다.

### 5.4 자동완성 — 3원칙과의 충돌 ①: 드롭다운이 아니라 여백의 낙서다

**이것이 §10(설계 테스트 요약)의 1번 질문("종이에 쓸 수 있는 것인가?")에
정면으로 걸리는 요소다.** 현대 UI의 자동완성 드롭다운은 둥근 모서리, 그림자,
흰 배경의 떠 있는 상자다 — 이 게임의 어떤 사물도 그렇게 생기지 않았다.
그대로 쓰면 이 문서 전체의 "책상이 화면이다" 원칙을 정면으로 어긴다.

**해소안**: 후보를 상자가 아니라 **입력창 바로 아래, 메모지의 연장선에
적힌 목록**으로 그린다. 상자의 경계를 없애고, 메모지와 같은 질감·같은
잉크로 이어 붙인다.

```css
.intake-suggestions {
  background: var(--paper-dim);
  border: none;
  box-shadow: none;                         /* 뜬 상자처럼 보이면 안 된다 */
  border-top: 1px dotted var(--ink-faint);  /* 절취선 느낌의 경계 */
}
.intake-suggestions li::before {
  content: "▸";                             /* 잉크 화살표 — 아이콘 폰트 금지 */
  color: var(--ink-faint);
}
```

후보가 0개일 때는 목록 자체를 그리지 않는다(빈 상자를 띄우지 않는다) —
paperwork-ux §4.3③의 "매칭 실패는 대가가 없다"를 시각적으로도 지킨다. 후보
0개가 일정 시간 지속되면 §5.6(길드마스터북)의 열람 제안이 여백에 작게 뜬다.

*설계 테스트 판정*: **조건부 통과 — 상자로 그리면 실패, 메모지의 연장으로
그리면 통과.** 구현 시 위 스니펫의 `box-shadow: none`과 `border: none`을
반드시 지킬 것. 프레임워크 기본 드롭다운 스타일을 그대로 쓰면 실패다.

### 5.5 단서 토큰 — 3원칙과의 충돌 ②: 색이 아니라 도장 모양으로 구분한다

**이것이 §4("강조색은 하나뿐이다")와 정면으로 부딪히는 지점이다.**
`design/research/paperwork-ux-2026-08-09.md` §4.2는 Golden Idol처럼 토큰
타입(시간/흔적/위험/지형)을 색으로 구분하자고 제안했다. 이 게임은 그렇게
할 수 없다 — 색을 네 개 더 쓰면 봉랍색의 무게가 사라지고, §10의 4번 테스트
(색각 이상에서 사라지는가)도 통과하지 못한다.

**해소안**: 타입을 **잉크 도장의 모양**으로 구분한다. 색은 전부
`--ink`(세피아 단색) 하나다.

| 타입 | 도장 모양 | 비유 |
|---|---|---|
| 시간 | 작은 모래시계 각인 | 언제 |
| 흔적 | 발자국 각인 | 무엇을 남겼나 |
| ~~위험~~ **징후** *(명명 변경 권고)* | 갈라진 선(균열) 각인 | 심상치 않음 |
| 지형 | 산 모양 각인 | 어디 |

> **이름 변경을 권고하는 이유**: paperwork-ux의 "위험" 타입 이름을 그대로
> 쓰면, 이 문서의 §4("위험은 무조건 붉은 봉랍")와 이름이 충돌해 "이건 왜 안
> 붉은가?"라는 혼란을 만든다. 이 토큰은 위험도 그 자체가 아니라 **위험을
> 암시하는 단서**이므로 세피아 각인으로 충분하다. 최종 명명(예: "징후",
> "불길한 조짐" 등)은 `game-designer` / `narrative-director`가 정할 것 —
> 이 문서는 시각 규칙만 제안한다.

의뢰서에 붙일 때는 봉랍이 아니라 **작게 눌러찍은 잉크 도장 자국**으로
그린다 — 밀랍 질감은 진짜 봉랍(§4)의 것을 아껴 둔다. 슬롯에 안 맞는 토큰은
물리적으로 놓을 수 없다는 것을 **그림자 실루엣**으로 미리 보여준다(맞는
모양의 빈 자리만 도장 실루엣이 옅게 찍혀 있다).

*설계 테스트 판정*: **조건부 통과 — 색이 아니라 모양으로 구분하면 §4를
어기지 않으면서 §10의 4번 테스트(색각 이상)도 함께 통과한다.**

### 5.6 길드마스터북 — 책상 위의 또 다른 책

책이므로 재질은 §7(UI 재질)의 나무 9-slice가 아니라 **가죽 장정 + 종이
내지**다. 펼쳐진 상태로 창구 한쪽에 겹쳐 놓이며, 상태 전환(접힘 ↔ 펼침)으로
"참고서를 펼쳤다"는 느낌을 준다.

내지는 반드시 **평문**이어야 한다(`game-concept.md` Anti-Pillars — "NOT
정답이 있는 적발 퍼즐"). 체크박스에 체크 표시가 들어가는 디자인은 피한다 —
이 책이 정답지처럼 보이면 안티 기둥을 시각적으로도 어기게 된다. 손 글씨체
라벨과 문장으로만 표시한다.

*설계 테스트*: "종이에 쓸 수 있는 것인가?" → **통과.** 책은 책상 위의
사물이다.

### 5.7 하루 행동 횟수 표시기 — 계기판이 아니라 티켓 뭉치

숫자 게이지나 프로그레스 바로 그리지 않는다 — 그 순간 이 문서의 문법이
아닌 현대 게임 HUD 문법이 끼어든다. 대신 **뜯어낸 티켓 반쪽** 또는 **잉크
눈금(tally mark)**의 뭉치로 그린다 — 오늘 쓸 수 있는 응대권을 한 장씩 뜯어
쓰는 그림이다.

- 남은 칸: 온전한 티켓 모양 또는 채워진 눈금
- 쓴 칸: 뜯겨 나간 자리(빈 윤곽선만) 또는 지워진 눈금
- **색은 세피아 하나다.** 칸이 얼마 안 남아도 붉은 봉랍색을 쓰지 않는다
  (§4 "비활성 사유에 쓰지 않는다"와 같은 원리 — 행동 소진은 위험이 아니라
  압박이다)

*설계 테스트*: "종이에 쓸 수 있는 것인가?" → **통과.** 티켓/눈금은 책상
사물의 은유다.

### 5.8 세력 평판 화면 — 여섯 개의 봉인이 찍힌 장부 면

세력 평판은 **회의실이나 지도가 아니라 장부의 한 면**으로 그린다 — "셈하는"
정보이지 "만나는" 장소가 아니기 때문이다(§1의 한 줄 규칙 그대로). 시민·귀족·
상인·범죄조직·모험가·왕실 각각을 **문장(紋章) 실루엣**으로 구분한다(색이
아니라 도형으로 — §5.5와 같은 원리를 재사용).

평판의 높낮이는 숫자나 색 그라디언트가 아니라 **봉인이 찍힌 개수 또는
봉인의 크기**로 표현한다 — 예를 들어 왕실 봉인이 크게 찍혀 있으면 왕실이
나를 강하게 의식하고 있다는 뜻이다. 이렇게 하면 "평판이 곧 선악 점수"로
오독될 여지가 줄어든다(`game-concept.md` Anti-Pillars — "NOT 선악 수치").

*설계 테스트*: "종이에 쓸 수 있는 것인가?" → **통과.** 장부 면은 종이
레지스터 안에 있다.

### 5.9 창 너머의 사람 — 창구 인물 뷰 *(2026-08-10 신설)*

> **이 절만이 「창」 레지스터를 다룬다.** §5.1~§5.8은 전부 종이다.
> *설계 테스트*: "종이에 쓸 수 있는 것인가?" → **실패.** 사람은 책상 위의
> 사물이 아니다. **그래서 종이가 아니라 창이며, 그 실패가 이 레지스터가
> 존재하는 이유다** (§10-1 참조).

#### 무엇인가

창구 화면 상단에, **창틀 너머로 의뢰인의 상반신이 보인다.** Papers, Please의
부스 창을 직접 참조한다 — 플레이어와 상대를 **가르는 경계**가 있고, 상대는
그 너머에 있으며, 서류는 이쪽 편에 있다.

**길드 홀의 픽셀을 옮겨온 것이 아니다** (§1의 창 ↔ 방 대조표). 홀은 위에서
내려다본 전신이고 창은 **눈높이 정면의 상반신**이다.

#### 배치 — 의뢰서를 이기지 않는다

| 영역 | 세로 비중 (1280×720 기준) | 레지스터 |
|---|---|---|
| 창 (창틀 + 인물 + 대사띠) | **상단 35~40%** | 창 |
| **카운터 상판** | 얇은 띠 — 두 레지스터의 경계선 | 경계 |
| 의뢰서 · 쪽지 · 청취록 · 티켓 | **하단 60~65%** | 종이 |

**§5.1은 불변이다** — 의뢰서가 여전히 단일 요소로서 가장 큰 면적을 갖는다.
창은 인물·창틀·대사띠를 합쳐서 상단을 쓰는 것이지, 인물 하나가 의뢰서보다
크다는 뜻이 아니다.

*설계 테스트 (§5.1의 것을 그대로 적용)*: 창을 가리고 봤을 때 화면이 여전히
"서류를 쓰는 화면"으로 읽히는가? 아니라면 창이 너무 크다.

#### 창은 조작 대상이 아니다

**인물을 클릭할 수 없다.** 창은 **읽는 곳**이지 누르는 곳이 아니다. 모든 조작은
카운터 아래(쪽지·의뢰서·청취록)에서 일어난다.

이유 둘. ① 인물이 클릭 가능해지면 창구가 홀처럼 **"가서 말을 거는" 화면**이
되어 §1의 창 ↔ 방 구분이 무너진다. ② 클릭 가능한 인물은 곧 **질문 메뉴**가
되고, 그것은 `intake-system.md` R1이 금지한 체크리스트화의 다른 얼굴이다.

> ⚠ 단어 앵커(대사 속 단어를 눌러 쪽지에 담는 것 — `intake-system.md` U2①)는
> **예외가 아니다.** 앵커는 인물이 아니라 **대사띠의 글자**에 걸린다. 글자는
> 종이 쪽 사물이다.

#### 기색 — 이 레지스터가 도입된 진짜 이유

`intake-system.md` R8의 **기색**(은폐 게이트가 열릴 수 있다는 유일한 신호)을
**표정으로** 전달한다. 텍스트로만 흘리면 놓치기 쉽다는 것이 2026-08-10
설계 리뷰의 반복 지적이었다.

| 표정 | 언제 | 무엇이 달라지는가 |
|---|---|---|
| **평상** | 기본 | — |
| **기색** | 게이트의 부모 노드가 열릴 때 | **눈** — 시선이 비껴간다. 눈동자 픽셀 1~2개의 이동 |
| **막힘(무지)** | 무지 종단 | **눈썹** — 미안한 듯 처진다 |
| **막힘(은폐)** | 잠긴 게이트 | **입** — 다물린 직선 |

**규칙 셋:**

1. **눈·눈썹·입 세 부위만 바꾼다.** 자세·의상·색은 고정. 부위를 늘리면 표정이
   연기가 아니라 아이콘이 된다
2. **표정은 붉은색을 쓰지 않는다.** 은폐도 위험이 아니라 정보의 부재다
   (§4 불변 — `intake-system.md` V절이 이미 같은 판정을 내렸다)
3. **표정이 사유를 앞질러 알려주지 않는다.** 기색 표정은 *"뭔가 있다"*까지만
   말하고 *"이것은 은폐다"*는 말하지 않는다 — 무지/은폐의 구별은 **대사를
   읽은 뒤** 잉크 얼룩 모양(§5.5 원리, `intake-system.md` V3)이 맡는다.
   이것은 같은 문서가 소리에 대해 내린 판정(스팅어를 사유별로 가르지 않는다)과
   **같은 규율**이다

#### 재질과 척도

- **창틀**: §7의 나무 9-slice를 재사용한다 (`ui-panel.png` 계열). 새 재질을
  만들지 않는다 — 창구의 나무 창틀과 길드 홀 UI가 같은 나무여야 **한 건물**로
  읽힌다
- **인물**: 원본 38×38 얼굴(Ninja Adventure faceset). **정수배 확대만** 한다
  (4× = 152, 5× = 190, 6× = 228). 비정수배는 픽셀을 뭉갠다.
  기본값 **5×**, 창 높이에 맞춰 4~6× 사이에서 조정
- `image-rendering: pixelated`는 **창 내부에만** 건다. §7의 규칙 그대로
  **글자에는 절대 걸지 않는다** — 대사띠는 창 안에 있어 보이지만 텍스트
  레이어는 별개다
- **창 안의 배경**: 단색 또는 아주 얕은 실내 암부. 타일·가구를 그리지 않는다 —
  그리는 순간 방이 된다

#### 캐스트 — 팩이 하나이므로 겹쳐도 된다

창과 방이 **같은 팩(Ninja Adventure)** 을 쓰므로, 한 인물이 창구와 길드 홀에
모두 나와도 **같은 얼굴로 보인다.** 캐스트를 분리할 이유가 없다.

> 검토 중 한때 창구에 다른 초상 팩을 들이는 안이 있었고, 그 경우 *"같은 사람이
> 두 곳에서 다르게 생긴다"* 를 막기 위해 **캐스트 분리 규약**이 필요했다.
> 팩을 하나로 유지하기로 하면서 그 제약이 통째로 사라졌다. **이것이 단일 팩을
> 고른 이유 중 하나다** — P6의 관계 기억(옛 의뢰인이 나중에 홀에 나타남)이
> 아무 사전 작업 없이 가능해진다.

#### 에셋

| | |
|---|---|
| 팩 | **Ninja Adventure Asset Pack** (pixel-boy, AAA) — **홀과 같은 팩** |
| 라이선스 | **CC0 1.0** — 표기 의무 없음 (`docs/asset-credits.md`에 기록은 남긴다) |
| 원본 | `Actor/Character/*/Faceset.png` — **50명 이상.** 현재 파이프라인은 24명만 뽑고 있으므로 창구용으로 확장한다 |
| 크기 | 38×38 얼굴. 창에서는 4~6배 정수 확대 |
| 왜 이것인가 | ① **홀과 화풍이 완전히 같다** — 창과 방이 한 건물로 읽힌다 ② CC0라 라이선스 리스크가 0이고 2차 저작(어깨·표정)이 자유롭다 ③ 파이프라인(`tools/asset-pipeline/build-hall-assets.cjs`)이 이미 있다 ④ 캐스트 분리 제약이 사라진다 |

**우리가 만들어야 하는 것 둘** — 팩에 없다:

1. **어깨/상반신** — faceset은 목 위만 있다. 같은 인물의 탑다운 스프라이트에서
   딴 옷 색으로 단순한 어깨 실루엣을 합성한다. 창틀 하단이 가슴 높이를
   자르므로 **정교할 필요가 없다**
2. **표정 4종** (평상·기색·무지·은폐) — 위 「기색」 절의 3부위 규칙대로 굽는다.
   38px에서 눈은 2×2픽셀 수준이라 변화가 미세하다 — **자세·고개 각도를 함께
   쓰는 것을 허용한다** (고개를 살짝 돌리는 것이 눈동자 1픽셀보다 잘 보인다)

> ⚠ **38px가 5배로 커졌을 때의 인상은 실측으로 확인했다** (2026-08-10 목업).
> 그럼에도 이 크기가 빈약하다는 판정이 나오면 **표정 표현력이 먼저 무너진다** —
> 그때의 대안과 기각 기록은 `docs/asset-credits.md`의 「창구 인물 검토」 절.

---

## 6. 방의 규칙 (길드 홀)

### 6.1 척도

- **타일 16px.** 사람도 16px — **사람 한 명 = 한 칸**이다.
- 방은 **31×16칸 (496×256px)**, 화면 폭에 맞춰 비율 유지로 늘린다.
- 사람이 두 칸을 넘으면 안 된다. 32px 에셋을 쓰지 않는 이유가 이것이다 — 16px
  의자·탁자 옆에 서면 거인이 된다.

*설계 테스트*: 새 인물 에셋이 의자보다 큰가? 그러면 이 방에 못 들어온다.

### 6.2 조명이 넓이를 만든다

**이것이 이 화면에서 가장 중요한 장치다.**

출석자는 많아야 6명인데 방은 31칸이다. 조명이 없으면 그냥 텅 빈 창고로 보인다.
화덕에서 퍼지는 따뜻한 빛과 사방으로 깔린 어둠의 대비가 있어야, 어두운 가장자리가
**"비어 있다"가 아니라 "보이지 않는다"** 로 읽힌다.

```css
background:
  radial-gradient(ellipse 34% 46% at 50% 6%, rgb(255 186 94 / 30%), transparent 62%),
  radial-gradient(ellipse 80% 90% at 50% 45%, transparent 38%, rgb(10 7 4 / 62%) 100%);
```

이 오버레이는 사람 위에 덮이므로 `pointer-events: none`이 **필수다.** 빠뜨리면 방
전체가 눌리지 않는다.

### 6.3 자리가 소속을 말한다

길드원은 **안쪽 탁자**, 외부인은 **문가**에 선다. 배지를 읽기 전에 위치로 먼저
읽힌다. 색만으로 구분하면 색각 이상에서 사라지고, 배지는 읽어야 한다 — 자리는
그냥 보인다.

신호는 세 겹으로 겹친다: **자리 → 이름표 색 → 배지 텍스트.**

### 6.4 자리 순서가 채워지는 순서다

출석이 4명이면 넓은 방에 흩뿌리지 않고 **화덕 아래 주 탁자부터** 채운다. 무리가
먼저 생겨야 "삼삼오오 모여 있다"로 보이고, 흩어지면 "썰렁하다"로 보인다.

자리 좌표는 **가구를 배치한 코드가 유일한 출처다**
(`tools/asset-pipeline/build-hall-assets.cjs` → `src/data/hall-layout.json`).
의자를 놓은 코드와 사람을 앉히는 코드가 다르면 반드시 어긋난다.

### 6.5 같은 사람은 같은 얼굴

아틀라스 인덱스를 인물 `id` 해시에서 유도한다. 무작위로 뽑으면 재렌더마다 얼굴이
바뀌고, 그러면 **성격 필터를 학습 가능하게 만드는 연결이 얼굴에서부터 끊긴다.**

---

## 7. UI 재질

### 나무 9-slice

대화창 패널·초상 액자·버튼은 팩의 `Theme Wood`를 CSS `border-image`로 쓴다.
직접 그린 테두리가 아니라 **실제 에셋**이며, hover/disabled 상태도 각각 다른
이미지다.

```css
border-image: url("../../assets/ui-panel.png") 5 fill repeat;
```

**글자에는 픽셀 보간을 걸지 않는다.** 틀은 `image-rendering: pixelated`지만 자식
요소는 `auto`로 되돌린다 — 한글이 뭉갠다.

```css
.hall-dialogue { image-rendering: pixelated; }
.hall-dialogue > * { image-rendering: auto; }
```

### 종이 화면의 UI

창구·결과·결산은 나무 틀을 쓰지 않는다. 얇은 잉크 선과 점선, 굵은 먹선 강조만
쓴다. §1의 두 레지스터 분리가 여기서 지켜진다. **2026-08-09 확인**: 청취
입력창·자동완성·의뢰서 판단 칸·단서 토큰·길드마스터북·행동 횟수 표시기·세력
평판 화면 전부 이 규칙을 따른다 — 세부 규칙은 §5.

---

## 8. 하지 않는 것

| 금지 | 이유 |
|---|---|
| **canvas / WebGL** | 프로젝트 결정 (`technical-preferences.md`). DOM + CSS로 충분하다 |
| **런타임 타일 조립** | 방은 변하지 않는다. 미리 구운 PNG 한 장(7.7KB)이면 되고, DOM 노드가 배경 하나로 끝난다 |
| **인물별 개별 파일** | 30명이면 파일 60개다. 아틀라스 두 장 + `background-position`으로 끝낸다 |
| **재배포 금지 에셋** | 공개 저장소 + GitHub Pages라 파일이 그대로 배포된다. CC0만 쓴다 |
| **원본 팩 커밋** | 94MB 중 19KB만 쓴다. 원본을 넣으면 무엇이 실제로 쓰이는지 알 수 없게 된다 |
| **화풍 혼합** | 인물 에셋은 한 팩에서만. UI 틀은 다른 팩에서 와도 되지만(액자와 그림의 관계) 인물끼리 섞으면 조잡해진다 |
| **자동완성을 상자·그림자로 그리기** *(2026-08-09 추가)* | 종이 레지스터를 깨는 현대 UI 관례. §5.4 참조 |
| **단서 토큰·세력 평판을 색으로 구분하기** *(2026-08-09 추가)* | 강조색 하나 원칙(§4)을 깨고 색각 이상 접근성도 깬다. §5.5, §5.8 참조 |

---

## 9. 로드 예산

| 항목 | 크기 |
|---|---|
| `hall-room.png` | 7.7KB |
| `cast-faces.png` (24명) | 7.7KB |
| `cast-sprites.png` (24명) | 2.5KB |
| UI 9-slice 5장 | 0.9KB |
| **합계** | **18.9KB** |

전체 예산은 gzip 200KB이고 현재 번들은 JS 21KB + CSS 7KB 수준이다. **에셋을 몇 배로
늘려도 예산에 여유가 있다** — 다만 늘릴 이유가 생겼을 때만 늘린다.

> **2026-08-09 확인**: §5의 새 요소(청취 입력창, 자동완성 목록, 단서 토큰
> 도장, 길드마스터북, 행동 횟수 티켓, 세력 평판 문장)는 전부 CSS/DOM으로
> 그린다 — 새 이미지 에셋이 필요 없다. 예산에 영향이 없다.

---

## 10. 설계 테스트 요약

새 시각 요소를 넣기 전에 통과해야 하는 질문들.

1. *(2026-08-10 갱신 — 2분법 → 3분법)* 이 요소는 **종이인가 창인가 방인가**?
   섞고 있지 않은가? 판정 순서는:
   ① **책상 위에 놓을 수 있는 사물인가** → 종이
   ② **눈높이 정면에서 보는 사람인가** → 창 (창구 상단에만 존재)
   ③ **내려다보며 돌아다니는 공간인가** → 방 (길드 홀에만 존재)
   ⚠ 창은 **창구 상단 띠에서만** 유효하다. 다른 화면에 창을 만들지 않는다 —
   레지스터가 넷이 되는 순간 §1의 규율은 규율이 아니게 된다
2. 이 요소가 **붉어야 할 만큼 치명적**인가?
3. 새 인물 에셋이 **의자보다 큰가**?
4. 정보를 **색 하나로만** 전달하고 있지 않은가? (색각 이상에서 사라지는가)
5. 이 그림의 **라이선스가 재배포를 허용**하는가?
6. 런타임에 조립하고 있는데, 사실 **변하지 않는 것**이 아닌가?
7. *(2026-08-09 추가)* 이 요소가 목록형 UI라면 — **둥근 모서리·그림자·떠
   있는 상자**로 그리고 있지 않은가? (§5.4 — 종이 레지스터에서는 실패다)
8. *(2026-08-09 추가)* 타입·카테고리를 구분해야 한다면 — **색이 아니라
   모양·도장·위치**로 구분할 수 있는가? (§5.5, §5.8)

---

## 11. 아직 적용되지 않은 것

정직하게 남긴다.

- **창구·파견·결과 대조·결산** 화면은 아직 이전 스타일이다.
  > **2026-08-10 정정 — 창구의 정리 방향이 뒤집혔다.** 이 항목은 원래 창구의
  > 픽셀 실루엣·창틀을 *"§1의 종이 레지스터와 섞여 있으니 **종이 쪽으로**
  > 정리하라"* 는 뜻이었다. §1 개정으로 **창구 상단은 이제 정당한 「창」
  > 레지스터**다. 정리해야 할 것은 픽셀의 존재가 아니라 **경계의 부재**다 —
  > 카운터 상판 위/아래로 창과 종이가 갈리고 있는가(§5.9)가 판정 기준이다.
  > **파견·결과 대조·결산은 종이 그대로다** — 창을 만들지 않는다 (§10-1).
- **의뢰인 얼굴**: 창구의 의뢰인은 아직 인라인 SVG 실루엣이다. `cast-faces.png`를
  쓰면 홀의 인물과 같은 규칙으로 통일된다.
  > **2026-08-10 확장.** 이 항목이 §5.9의 씨앗이었다 — 다만 범위가 **얼굴
  > 하나**였다. 지금은 창 프레임·5배 확대·표정 4종·어깨 합성까지 포함한다.
  > 파이프라인 작업 셋: ① faceset 추출을 24명 → **50명+**로 확장
  > ② 어깨 실루엣 합성 ③ 표정 4종 변형 굽기. 전부
  > `tools/asset-pipeline/build-hall-assets.cjs` 계열에서 처리한다.
- **§5 전체가 미구현이다** *(2026-08-09 추가)*. 청취 입력창, 자동완성 목록,
  받아쓰기/판단 칸 분리, 단서 토큰 도장, 길드마스터북, 하루 행동 횟수
  표시기, 세력 평판 화면 — 이 문서가 규칙을 앞서 정의했을 뿐, `production/roadmap.md`
  P1(청취·의뢰서), P3(행동 횟수), P5(세력 평판) 구현 전까지는 실제 화면이
  없다. 규칙과 실체가 갈리지 않도록, 구현 시 §5의 각 하위 절 번호를 커밋
  메시지나 스토리 설명에 인용할 것을 권장한다.
- **소리 없음**: 오디오는 이 회차 범위 밖이다 (`production/roadmap.md` P8).
