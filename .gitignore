# === 빌드 산출물 ===
node_modules/
dist/

# === 로컬 전용: 개발 도구 설정 ===
# Claude Code 스킬·에이전트 정의(프로젝트 템플릿). 로컬에서만 쓰고 저장소에는 올리지
# 않는다 — 파일 211개가 게임 소스를 가린다. AI 활용 내역은 제출물 4번 문서로 낸다.
.claude/
CLAUDE.md
CLAUDE.local.md

# === 로컬 전용: 세션 상태 ===
# 이 둘만 무시한다 — 매 세션 갈아엎히는 임시 기록이고 머지 충돌만 만든다.
# production/stories/ 는 추적한다: 스토리 19개가 이 프로젝트의 생산 계획 전체이며,
# AC와 구현 판정 기록이 거기 있다. 무시하면 클론에서 계획이 통째로 사라진다.
production/session-state/
production/session-logs/

# === 에디터 / OS ===
.vs/
.vscode/settings.json
.idea/
*.swp
.DS_Store
Thumbs.db
Desktop.ini

# === 로그 / 캐시 ===
*.log
*.tmp
.npm/

# === 비밀 ===
.env
.env.local
.env.*.local
*.pem
*.key
# LLM 프로토타입의 로컬 API 키 (config.example.js만 추적한다)
prototypes/llm-intake-ab/config.js

# 외부 CC0 에셋 원본 아카이브. 저장소에 넣지 않는다 —
# tools/asset-pipeline이 여기서 필요한 조각만 구워 src/assets/에 내보낸다.
# 출처와 라이선스는 docs/asset-credits.md 참조.
*.zip
