# 스모크 검증 — 2026-08-09 (중간 배포)

> **story-019의 최종 사인오프가 아니다.** story-017(시각 정리)과 story-018(밸런스)이
> 진행 중이며, 그 둘이 끝나면 재배포하고 이 문서를 갱신한다. 이 검증의 목적은
> 컨셉 문서가 Scope Risk로 지목한 *"Day 3에 첫 배포를 시도하는 재앙"* 을 미리
> 제거하는 것이다 — 게임이 올라간 상태에서 파이프라인이 도는지 지금 확인해 둔다.

## 배포 정보

| 항목 | 값 |
|---|---|
| URL | https://minsu42.github.io/help-wanted/ |
| 커밋 | `0e4bff4` (main, origin 동기화됨) |
| 푸시 전 검증 | `npm run check` 통과 (typecheck → test 382개 → build) |

## 응답 코드 — `base: "./"` 작동 확인

| 코드 | 전송량(gzip) | 경로 |
|---|---|---|
| 200 | 312B | `/` |
| 200 | 18,835B | `/assets/index-C1masFzC.js` |
| 200 | 2,405B | `/assets/index-D2GMFNEg.css` |

- 배포본의 에셋 해시가 로컬 빌드 산출물과 **정확히 일치**한다 (`index-C1masFzC.js` /
  `index-D2GMFNEg.css`) — 오래된 빌드가 서빙되고 있지 않다는 증거다.
- **404 0건.** 저장소 하위 경로 배포에서 에셋이 뜬다 = `vite.config.ts`의
  `base: "./"`가 유지되고 있다.

## 로드 예산

| 항목 | 실측 | 예산 | 여유 |
|---|---|---|---|
| gzip 합계 (HTML+JS+CSS) | **21.6KB** | 200KB | 예산의 **11%** |

런타임 의존성 0개를 유지한 결과다. 번들의 거의 전부가 우리 코드다.

## 콘솔 / 네트워크

- 콘솔 에러 **0건**, 경고 0건
- 네트워크 요청 3건, 전부 200. 외부 호스트 요청 없음 (정적 호스팅 제약 준수)

## 완주 확인 (배포본)

### ① 15일 완주 → 결산 도달
창구에서 "하루를 마감한다"를 반복해 15일을 소진. **15회 후 결산 화면 도달**,
16일차가 존재하지 않음. 결산에 시드(`3799043863`), 최종 명성·자금·등급·길드원 수,
운명 텍스트, 사망자 0명 전용 문구("이번 회차 동안 아무도 잃지 않았다") 표시 확인.

### ② 핵심 루프 완주 (홀 → 흥정 → 파견 → 결과 대조)
재시작 후: 길드 홀 전원 대화 → 창구 복귀 → 의뢰 제안 → 파티 배정 → 하루 경과 →
결과 대조 화면 도달. 그 화면에서 확인된 것:

- **소문 화자 귀속이 작동한다** — "유나 뮐러(냉소적이다·탐욕스럽다)에게서 들었다",
  "당신은 유나 뮐러의 말을 믿었다"
- **성격 왜곡이 눈에 보인다** — 들은 위험도 45 vs 실제 39. `bitter`가 나쁜 쪽으로
  부풀린 값이 좌우 대조로 드러난다. 이것이 "다음엔 깎아 듣자"를 가르치는 지점이다
- **얻지 못한 정보가 빈칸이 아니다** — "몰랐다 — 소문을 얻지 못한 채 판단했다"
- **은폐 표식** — "당신은 실제 위험을 알고 있었지만 계약서에 적지 않았다"
- **마진이 숫자가 아니라 띠로 표시된다** — "아슬아슬한 도박이었다"
- **미지급 처리** — 잔금 미수령 + 의뢰인 지불 여력 31% 영구 공개 안내
- `capability` 원본 숫자는 화면에 나오지 않고 등급("한몫")만 나온다

## story-019 AC 대비 현황

| AC | 상태 |
|---|---|
| `npm run check` 통과 | ✅ |
| GitHub Actions 워크플로 성공 | ✅ (배포본 해시 일치로 확인) |
| 배포 URL에서 게임 로드 | ✅ |
| `index.html`/`css`/`js` 전부 200 | ✅ |
| 배포본에서 15일 완주 가능 | ✅ |
| 콘솔 에러 0건 | ✅ |
| gzip 200KB 이하 | ✅ 21.6KB |
| 커밋 기록이 작업 단위로 남아 있다 | ✅ 스토리 단위 커밋 |
| 첫 화면 1초 이내 | ⬜ 미계측 (체감상 즉시. 정식 계측은 최종 배포 시) |
| 저장소 공개 또는 심사 계정 초대 | ⬜ **미확인 — 사용자 확인 필요** |

## 남은 것

1. story-017 / story-018 완료 후 **재배포하고 이 문서 갱신**
2. 저장소 공개 상태 확인, 비공개면 `dl_gameai_reviewer@nhn.com` 초대
3. 첫 화면 표시 시간 정식 계측
