# Tech Debt Register

> **2026-08-09 개정 — 3일 마감 전제 해제. 부채의 "지금은 못 갚는다"가 더 이상 성립하지 않는다.**
>
> 이 문서의 항목들은 전부 *"3일 예산이 없어서"* 미룬 것이었다. `design/gdd/game-concept.md`
> 전면 개정(2026-08-09)으로 시간 제약이 사라졌으므로 **미룸의 근거가 소멸했다.**
> 각 항목은 이제 "왜 아직 안 갚았는가"를 새로 답해야 하며, 답이 없으면
> `production/roadmap.md`의 P0에서 갚는다. 아래 개별 항목에 개정 주석을 달았다.

여기 있는 항목은 **지금 고치지 않기로 의식적으로 결정한 것**이지, 발견하지 못한 것이
아니다. 각 항목은 발견 시점의 스토리로 추적된다.

우선순위 기준: 게임이 동작하지 않는 것은 여기 오지 않는다(그건 버그다). 여기 있는 것은
전부 "동작하지만 나중에 물릴 수 있는 것"이다.

---

## Open

- **2026-08-08** (Story 002 — 의뢰 생성): 유혹 의뢰의 배수 적용 지점이 **설계 판단으로
  결정됐고 아직 검토받지 않았다.** 현재 구현은 위험 1.6배 · 총 보상 3.2배(= 위험당 단가
  2배)다. 대안 해석은 총 보상 2.0배(단가 1.25배)이며 유혹의 강도가 크게 달라진다.
  economy-designer 검토 대상. 뒤집으려면 `contract.ts`의 `baseReward` 한 줄이다.
  출처: `production/stories/story-002-contract-generation.md`

- **2026-08-08** (Story 002 — 의뢰 생성): `knownBy` 가중치가 `tenureYears^1.5`이므로
  **근속 0년은 영영 뽑히지 않는다.** 기본 명부에서 2~3명이 정보망에서 완전히 제외된다.
  "갓 들어온 사람이 상단주를 알 리 없다"는 해석으로 두었으나 systems-designer 확인이
  필요하다. 바꾸려면 가중치를 `(tenureYears + 1)^exponent`로 옮기면 된다.
  출처: `production/stories/story-002-contract-generation.md`

  > **2026-08-09 주석**: 로드맵 P4가 **소문의 출처를 길드원 → 외부인·정보상으로
  > 옮긴다.** 그러면 `knownBy` 가중치가 길드원 근속에 걸리는 구조 자체가 바뀌므로,
  > 이 항목은 지금 고치기보다 P4의 `rumor.ts` 개정에 합쳐 처리하는 것이 싸다.
  > 단 P4 이전에 밸런스 측정을 돌린다면(active.md의 "첫인상 리스크") 이 편향이
  > 측정값을 오염시킨다는 점은 유효하다.

## 문서 불일치

- **2026-08-08** (story-007 빌드 중 발견): **`package.json`의 Vite가 `^7.3.6`인데
  `docs/engine-reference/web/VERSION.md`는 "Vite 5.4로 고정"이라고 적고 있다.**
  그 문서는 5.4 고정을 근거로 Risk Level을 LOW로 매기고, 버전을 올리면 즉시 HIGH가
  된다고 경고한다 — 실제로는 이미 올라가 있다.

  빌드·테스트·배포는 정상 동작하므로 **당장의 위험은 없다**(193 테스트 통과, 프로덕션
  빌드 129ms, 번들 9.8KB gzip). 다만 `^`가 붙어 있어 다음 설치에서 또 올라갈 수 있고,
  그때는 조용히 깨질 수 있다. 마감 후 둘 중 하나로 정리할 것 — VERSION.md를 실제
  버전으로 갱신하거나, package.json을 문서대로 되돌리거나. ~~**지금 건드리면 3일 예산에
  "빌드 설정 디버깅"이 들어온다.**~~ → **폐기 (2026-08-09).** 3일 예산이 없어졌으므로
  미룸의 유일한 근거가 사라졌다.

  > **2026-08-09 — ✅ 해소됨.**
  >
  > 조치: 문서를 **실측값으로 정정**하고 `package.json`의 `^`/`~`를 **정확한 핀**으로
  > 바꿨다. 검증: `npm run check` 통과 (테스트 407/407, Vite 7.3.6 빌드 성공,
  > gzip JS 21.03KB + CSS 7.29KB).
  >
  > | 패키지 | 핀 |
  > |---|---|
  > | typescript | 5.5.4 |
  > | vite | 7.3.6 |
  > | vitest | 4.1.10 |
  > | happy-dom | 20.11.2 |
  >
  > **상향(TS 7 / Vite 8)은 하지 않았다.** VERSION.md의 고정 근거 3개 중
  > *"LLM이 API를 지어내지 않는다"* 하나가 살아남았고, 장기 개발 + 에이전트 주도
  > 작업 방식에서 오히려 더 무거워졌기 때문이다. 재검토는 로드맵 GATE 통과 후.
  > 결정 기록: `production/roadmap.md`「버전 정책」.
  > VERSION.md의 고정 근거 3개 중 2개가 3일 마감에 기대고 있었으므로 그 논리 자체를
  > 재검토해야 한다. 결정 전까지 이 문서가 **실측값의 유일한 기록**이다.

## Open — 고쳐야 하는 것

- **2026-08-09 발견 — 배정 화면에 탈출구가 없다 (게임 정지).**
  가용한 모험가가 전부 배정을 거부하면(`member.trust <
  assignmentRules.assignmentTrustThreshold`, `DispatchScreen.ts:178`) 체크할 사람이
  없어 `confirm`이 영구히 disabled인데, **assign 단계가 렌더하는 `data-action`이
  `confirm` 하나뿐이라** 화면을 벗어날 방법이 없다. `return`(창구로 돌아간다)은
  결과 단계에만 있다.

  **측정**: 자동 플레이 6회 시행 — P0 이전 빌드(b631f1a) **6/6 막힘**, P0 이후 3/6.
  시드가 `Date.now()`(`main.ts:151`)라 확률적으로 발생한다. **P0가 만든 것이 아니다.**

  ⚠ `production/roadmap.md`의 **「main은 항상 배포 가능한 상태여야 한다」**와 정면으로
  부딪히며 제출 마감이 임박했다.

  고치려면 설계 결정이 필요하다 — 나가면 그 의뢰는 파기되는가 열린 채 남는가,
  이미 타결된 `Settlement`과 `state.offersMade`는 어떻게 되는가, 전원 거부가 애초에
  정상 상태인가(`assignmentTrustThreshold`가 0.15로 너무 높을 수 있다), 신뢰가 전부
  바닥나는 것 자체가 패배 조건이어야 하는가. **로드맵 P4(배정 거부와 그 결과)가 이
  영역을 정면으로 다루므로 거기서 같이 설계하는 것이 옳을 수 있다** — 다만 마감
  전이라면 응급 처치(창구 복귀 버튼)를 먼저 넣는다.

## Deferred — 의식적으로 미룬 확장

- **2026-08-08 — LLM 서술 레이어 (story-019 이후 검토).** 대사를 런타임에 LLM으로
  생성하는 안을 검토했고 **결과 대조 화면(story-014) 한 지점에만, 배포 스모크 이후에**
  얹기로 했다.

  > **2026-08-10 — 최종 종결: 부채가 아니라 본선이 됐다 (ADR-003).**
  >
  > 아래 WONTFIX가 적어 둔 되살릴 조건("정적 호스팅 제약 폐기 = 새 결정")이 사용자
  > 결정으로 충족됐다. 이 항목이 2026-08-08에 제안했던 것(프록시 + 캐시 + 폴백)은
  > **부채 상환이 아니라 ADR-003의 정식 아키텍처로 채택**됐고, 범위는 결과 대조
  > 한 지점이 아니라 **청취의 이해·연기 전체**로 커졌다. 이 항목에서 지금도 유효한
  > 통찰 둘 — **"폴백이 필요하므로 템플릿 엔진은 어차피 만들어야 한다"** (= ADR-003
  > D5), **"대기 1~2초가 오히려 무게를 싣는 순간에 쓴다"** (= D7의 뜸 연출).
  > 이 항목은 레지스터에서 종결이며 후속은 ADR-003이 소유한다.

  > **2026-08-09 — WONTFIX로 종결. 이 항목은 더 이상 부채가 아니다.**
  >
  > 아래 「결론을 내지 않고 표시만 해 둔다」의 판정을 내렸다. **컨셉이 채널 자체를
  > 막았으므로 문자 그대로 폐기 대상이 맞다.** 정적 호스팅에는 서버가 없고, 프록시를
  > 세우는 것은 미룬 일을 하는 것이 아니라 **배포 아키텍처를 바꾸는 일**이다.
  > 부채로 남겨두면 "언젠가 할 일"의 자리를 계속 차지한다.
  >
  > 되살릴 조건은 하나뿐이다 — 정적 호스팅 제약이 폐기될 때. 그때는 부채 상환이
  > 아니라 새 결정이다. **빌드 타임 LLM 어휘 생성은 그대로 유지된다.**
  > 기록: `production/stories/story-006-text-assembly.md`, `production/roadmap.md`
  > 「미해결」 표의 P0 항목.

  > **2026-08-09 — 기준 문서와 충돌한다. ~~결론을 내지 않고 표시만 해 둔다.~~**
  >
  > 전면 개정된 `design/gdd/game-concept.md`가 「넘을 수 없는 제약」 1번으로
  > *"서버가 없다. 외부 API를 호출할 수 없다"* 를 **타협 불가**로 선언했고,
  > 안티 기둥에 *"NOT LLM 자유 대화"* 를 넣었다. 반면 이 항목은
  > *"프록시(Cloudflare Worker)를 세우면 기술적 장애물은 없다"* 고 적고 있다.
  >
  > 둘은 같은 층위가 아니다 — 컨셉이 금지한 것은 **판정에 개입하는 자유 대화**이고,
  > 이 항목이 제안한 것은 **판정 밖의 서술 텍스트**다. 그러나 컨셉의 제약 문장은
  > 채널 자체("외부 API 호출")를 막고 있으므로 **문자 그대로 읽으면 이 항목은 폐기
  > 대상**이다. 미룸의 근거였던 "일정"은 이미 소멸했으므로, 남은 것은 순수한
  > 설계 판단이다. **technical-director 판정 대기.**

  기술적 장애물은 없다 — 프록시(Cloudflare Worker)를 세우면 API 키 노출은 해결되고,
  서술 텍스트는 판정에 들어가지 않으므로 결정론도 깨지 않는다 (`(seed, eventId)`
  캐싱으로 재생도 안정적).

  **미루는 이유는 일정이다.** 남은 스토리 14개 추정 합이 20.5h인데 실질 가용은
  14~16h로 이미 초과 상태이고, LLM 레이어는 프록시 2~4h + 프롬프트 안정화 3~6h +
  캐시·폴백 1~2h로 6~12h를 더한다. 게다가 폴백이 필요하므로 **템플릿 엔진(story-006)은
  어차피 만들어야 한다** — 대체가 아니라 추가다.

  결과 대조를 고른 이유: 호출량이 파견당 1회로 적고, 결과를 보는 순간은 1~2초 대기가
  오히려 무게를 싣고, 감정적 페이로드가 거기 있으며(*"당신은 사흘 전에도 그를
  보냈다"*), **왜곡 숫자가 없다.** 왜곡은 소문 쪽에 있고 결과 대조는 진실을 밝히는
  화면이라 LLM이 건드릴 수치가 없다.

  창구 흥정과 홀 대화는 제외한다 — 매 상호작용마다 호출이라 100ms 응답 예산과
  충돌하고, 소문에는 성격별 왜곡값이 실려 있어 LLM이 그 숫자를 뭉개면 *"저 친구 말은
  늘 과장이니 깎아 듣자"* 라는 학습 신호가 죽는다.

## Resolved

- **2026-08-08 해결** (Story 001에서 발견, Story 002에서 해소): `names.json` 정적
  import. `src/domain/person.ts`를 신설해 `NamePool`을 주입받도록 바꿨다.
  의뢰인도 같은 이름 생성 로직이 필요해 어차피 공통화가 필요했고, 그 김에 해소됐다.
  **이름 고갈 throw 경로가 처음으로 테스트 가능해졌다**
  (`test_exhausted_name_pool_throws`). 부수 효과로 모험가와 의뢰인이 이름 집합을
  공유할 수 있게 되어 둘 사이의 동명이인도 막힌다.

- **2026-08-08 해결** (Story 001에서 발견, Story 002에서 해소): `tenureYears` 경계값
  도달 검증. `test_range_boundaries_are_actually_reachable`이 이제 `capability`와
  `tenureYears` 양쪽의 최소·최대를 함께 확인한다. story-002의 `knownBy` 가중 추출이
  실제로 이 값을 쓰게 되면서 시급해진 항목이었다.

- **2026-08-08 해결** (Story 001에서 발견, Story 002에서 해소): `pickTwoTraits`의
  `TRAITS.length >= 2` 불변식. 주석 대신 명시적 가드를 넣어 도메인 메시지로 던진다
  (`성격 태그가 N개뿐이다 — 서로 다른 2개를 뽑을 수 없다`).

- **2026-08-08 해결** (Story 001에서 발견, 전 스토리 영향): story-002~019의 Test
  Evidence 경로가 전부 `*_test.ts` 형식이라 `vitest.config.ts`의
  `include: ["tests/**/*.test.ts"]`에 수집되지 않았다. 그대로 뒀으면 테스트가 실패하는
  게 아니라 **조용히 실행되지 않았다** — 침묵이라 알아채기 어려운 종류다. 10개 파일
  (002·003·004·005·006·009·011·012·013·015)을 `*.test.ts`로 일괄 정정했다.
  `trust_memory_test.ts`는 파일 명명 규칙(camelCase)에 맞춰 `trustMemory.test.ts`가 됐다.
